# やること

思いついた改善とリファクタリングはここに書き溜めて、上から順に処理する。
やり終えたら行を消し、必要なら「済んだこと」に理由だけ残す。

判断の根拠は次に置いてある。着手前に読む。

- 戦闘の設計 … `docs/battle_design.md`
- 潜る理由（前提） … `docs/premise.md`
- 作業の規約 … `AGENTS.md`

---

## 進み具合

**済んだこと 120 件 / 残り 10 件。**

終わった項目はここから消して `docs/tasks_archive.md` へ移すので、
このファイルに `[x]` は残らない。**動いているかどうかは下の「直近に済んだこと」で見る。**
理由まで読みたいときは控えのほうを開く。

### 直近に済んだこと

- **シミュレータが 1 回で捕まえた。** 目で見ても「6 体は多いかな」までしか
- 割り引きは挟み込みで決めた（0 → 全滅、5 → 全周 14%、8 → 全周 32%、
- **数だけ増やして強さを据え置くと成立しない。** 6 へ上げた直後、シミュレータで
- **3 に縛られていたものは 3 つあったが、どれも結果であって理由ではない。**
- **「なぜ 3 なのか」に答えが無かった。** git で元のファイルを確認したが、

## 次にやる（効き目の大きい順）

- [ ] **町が全部同じ形。** `TownGenerator` が宿・店・出口を固定座標に置いていて、
      変わるのは名前と人の位置と石畳の斑だけ。**「迷わせない」を固定配置で実装したのが
      間違い**で、迷わせないのは「出口が分かること」であって「毎回同じ間取り」では
      ない。区画を並べる方式（広場の位置・建物の数と向き・門の辺・水路や広場の
      有無）に変えて、`verify()` で宿・店・出口への到達を守れば形は自由に振れる。
      生物相ごとに材質を変えると、雪原の町と火山の町が別物に見える。
- [ ] **イベントが浅い。Codex のカタログを活かせていない。**
      いまは premise を 2 行出して選択肢を並べ、結果を toast 1 行で流すだけ。
      カタログ側には tags・trigger・fail_forward まで在るのに使っていない。足す:
      (1) 選んだ**あとの一文**（何が起きたかを語る。toast では流れて読めない）
      (2) 危険（risks）が実際に発火したときの分岐表示
      (3) 同じ世界で前に選んだことを覚えて、後のイベントの文へ反映
      (4) **物語の六拍を実際に踏ませる**（下の項目）
- [ ] **シナリオ六拍が生成されただけで、一度も画面に出ない。** これが「せっかく
      作ったのに台無し」の中身。`WorldMap.story` に在って `verify()` も通るのに、
      拍を踏んだときの表示・choice 拍の選択・epilogue の回収が繋がっていない。
      `EventView` を語り用にも使える形にしてあるので、拍の `line` と `choices`
      （`immediate_cost`/`preserves`/`sacrifices`）を流し込み、
      `resolve_ending()` の結果を戦記に載せる。**ここが最優先。**

- [ ] **アップグレードが貧相で、あるのと同じになっていない。**
      いま 4 種（旅支度・手の記憶・商いの伝手・備え）で、どれも「立ち上がりが
      少し楽」しか起きない。**恒久強化は能力値に触れない**という不変条件を守った
      結果として、触れる範囲が狭すぎる。能力値以外で効く軸を増やす:
      解禁（新しい職・新しい品）、選択肢（出撃前に持ち出す物を選ぶ）、
      情報（世界地図の一部が最初から見える）、保険（1 回だけ全滅を免れる）、
      拠点の設備（封の在り処が最初から 1 つ分かる）。
      **数を増やすより、段が進むと質が変わるものを 1 つ入れるほうが効く。**
- [ ] **中断機能（ラン途中で保存して閉じる）。** いまセーブは拠点の恒久データだけで、
      世界・位置・封・イベント・物語の進みは保存していない。長い 1 ランを
      途中で閉じられないのは、ワールド 1 周が 1 ランになった時点で無理がある。
      `WorldMap` と `GameState` のラン内資源を丸ごと書き出す（決定性があるので
      種と進行度だけでも復元できる可能性がある。どちらが安いか測ってから決める）。
- [ ] **デバッグ用の状態保存。** 「この世界のこの場面」を保存して読み直せると、
      不具合の再現とバランス確認が一気に楽になる。`--dev-save=名前` /
      `--dev-load=名前` で `user://dev/` へ。中断機能と同じ書き出しを使い回す。
- [ ] **あそびにんの絵だけ雰囲気が違う。** 他の 14 職と描き方が揃っていない。
      `HERO_LOOKS`（`tools/gen_assets.py`）の差分か、取り込んだ候補絵のどちらかが
      浮いている。並べて撮って原因を切り分ける。

- [ ] **検算層が 2 本ある。** `QuestText`（封の名）と `QuestNarrativeText`
      （イベントの表層）が、禁止語・数字・記号の表をそれぞれ持っている。
      片方だけ直す事故が起きるので 1 本へ寄せる。
- [ ] **イベントの効果 11 個が文だけで、まだ効かない。**
      `EventEffects.INFORMATIONAL` に明示してある（黙って無視ではない）。
      世界地図への書き込み（近道・道を失う・地図が分かる）、主戦の下準備
      （手が読める・弱る）、5 人目の仲間、宿の格上げ。**ここを空にするのが次。**
- [ ] **イベントに AI の表層を繋ぐ。** 骨格と `DetRng` の表層までは動いている。
      `WorldEventCatalog.facts_for_ai()` と `apply_ai_skin()` が既にあるので、
      `LocalAI` から呼んで `QuestNarrativeText` に通すだけ。封の名と同じ作り。

---

済んだものは `docs/tasks_archive.md` へ移す（このファイルは 200 行以内に保つ）。
