# やること

思いついた改善とリファクタリングはここに書き溜めて、上から順に処理する。
やり終えたら行を消し、必要なら「済んだこと」に理由だけ残す。

判断の根拠は次に置いてある。着手前に読む。

- 戦闘の設計 … `docs/battle_design.md`
- 潜る理由（前提） … `docs/premise.md`
- 作業の規約 … `AGENTS.md`

---

## 進み具合

**済んだこと 97 件 / 残り 0 件。**

終わった項目はここから消して `docs/tasks_archive.md` へ移すので、
このファイルに `[x]` は残らない。**動いているかどうかは下の「直近に済んだこと」で見る。**
理由まで読みたいときは控えのほうを開く。

### 直近に済んだこと

- **待たせない。** 世界はテンプレートの名前で完成済みで、数秒後に届いたら
- **実機で通した。** Ollama から JSON が返り、検算が 6 項目すべて採用、
- **HTTP の接続点を 1 つにした。** `LocalAI` だけが Ollama を叩き、戦記も
- **AI は構造に触らない**ことをテストで固定した。名と一文を当てたあとも
- **項目ごとに落とす。** 1 つ変でも全部捨てない。封の名が 1 つ駄目でも残り 2 つは

## 次にやる（効き目の大きい順）

---

済んだものは `docs/tasks_archive.md` へ移す（このファイルは 200 行以内に保つ）。
