# やること

思いついた改善とリファクタリングはここに書き溜めて、上から順に処理する。
やり終えたら行を消し、必要なら「済んだこと」に理由だけ残す。

判断の根拠は次に置いてある。着手前に読む。

- 戦闘の設計 … `docs/battle_design.md`
- 潜る理由（前提） … `docs/premise.md`
- 作業の規約 … `AGENTS.md`

---

## 進み具合

**済んだこと 155 件 / 残り 4 件。**

終わった項目はここから消して `docs/tasks_archive.md` へ移すので、
このファイルに `[x]` は残らない。**動いているかどうかは下の「直近に済んだこと」で見る。**
理由まで読みたいときは控えのほうを開く。

### 直近に済んだこと

- オート中は右上に作戦を出しているので、メッセージをその幅ぶん狭めて折り返す
- **選べるのに見えない状態を作っていた。** メニューが 8 項目になって「ぬけだす」と
- **用が済んだ洞から出られるようにした。** ただし封が残っている洞では使わせない
- **シミュレータにも番人を通した。** ここを飛ばすと、封を集める線が実際より楽に
- **封を置いたのに守るものが居なかった。** ただし城の主と同格にすると、寄り道が

## 次にやる（効き目の大きい順）

- [ ] **ゲートのフレーム勘定が取りこぼす。** 戦記で「テンプレート文」と「AI 文」が
      252px 重なると報告されるが、`ResultScreen._on_ai_written()` は事実 3 行＋AI 文で
      **置き換えている**ので、1 フレームの中では重なっていない。フレームの区切りを
      「最初の窓の再登場」で取っているのが弱く、画面によって取りこぼす。
      区切りは推測ではなく明示（各 View の `_draw()` 冒頭で `PixelUI.ui_frame()` を
      呼ぶ）に変えるべき。**「見分ける」より「印を付ける」**（開きかけの窓で学んだのと同じ）。
      いまの同一行の閾値 4px も、広げると誤検出が溢れるので狭いままにしてある
      （サイズ違いの接触は取りこぼす）。
- [ ] **Codex の新規素材を精査して組み込む。** `docs/cross_world_story_design.md`
      （複数ランをまたぐ物語）ほか、置かれた素材をまだ読んでいない。
      これまでの 2 件（イベントカタログ / シナリオ六拍）と同じ手順で:
      単体テストを走らせ、本体から呼ばれているかを確かめ、繋ぎ込みを分けて積む。

- [ ] **とうぞくの絵が忍者になっている。** 別の職として `ninja` があるので、
      とうぞくは軽装の盗人（頭巾ではなく目隠し布、短刀、袋）に描き分ける。
      `HERO_LOOKS`（`tools/gen_assets.py`）か取り込んだ候補絵のどちらかを直す。
- [ ] **1 戦が実時間で長い。** 敵 6 体の戦闘は自動プレイで 25 秒かかり、
      詰まり検出（同じ画面 25 秒）に引っかかった。**論理的な詰まりではない**
      ―― 手番数を測ると 1 体 1.3 / 3 体 8.0 / 6 体 11.6 手番で、必ず決着する。
      長いのは 1 手番あたりの実時間（およそ 2 秒）。
      速くする先は 3 つ: メッセージの待ち（`AUTO_LINE_DELAY` 0.28）、
      被弾の点滅、ダメージ数字の滞留。**オート中はまとめて詰める**のが筋で、
      自分で選んでいるときの間は残す（読む時間が要るのはそちらだけ）。
      触ったら `--play` で実測し、詰まり検出に出なくなることを確かめる。

---

済んだものは `docs/tasks_archive.md` へ移す（このファイルは 200 行以内に保つ）。
