# やること

思いついた改善とリファクタリングはここに書き溜めて、上から順に処理する。
やり終えたら行を消し、必要なら「済んだこと」に理由だけ残す。

判断の根拠は次に置いてある。着手前に読む。

- 戦闘の設計 … `docs/battle_design.md`
- 潜る理由（前提） … `docs/premise.md`
- 作業の規約 … `AGENTS.md`

---

## 進み具合

**済んだこと 135 件 / 残り 0 件。**

終わった項目はここから消して `docs/tasks_archive.md` へ移すので、
このファイルに `[x]` は残らない。**動いているかどうかは下の「直近に済んだこと」で見る。**
理由まで読みたいときは控えのほうを開く。

### 直近に済んだこと

- **返りの振り分けは「頼んだ側で印を立てる」。** 受け取る側で立てたら、返りが
- **イベントの表層も AI に書かせた。** `WorldEventCatalog.facts_for_ai()` が
- **世界がプレイヤーを覚えている。** 選んだ手の tag を数えておき、後のイベントで
- **危険は実際に振る。** 並べておいて起きないなら、それは危険ではなく飾りだった。
- **開きかけの窓の誤検出は、縮めた本人に印を付けさせるのが確実だった。**

## 次にやる（効き目の大きい順）

---

済んだものは `docs/tasks_archive.md` へ移す（このファイルは 200 行以内に保つ）。
