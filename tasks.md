# やること

思いついた改善とリファクタリングはここに書き溜めて、上から順に処理する。
やり終えたら行を消し、必要なら「済んだこと」に理由だけ残す。

判断の根拠は次に置いてある。着手前に読む。

- 戦闘の設計 … `docs/battle_design.md`
- 潜る理由（前提） … `docs/premise.md`
- 作業の規約 … `AGENTS.md`

---

## 完了Gate

- **コードや画像が「ある」だけでは完了にしない。** 実行時に通る経路と結果を見る。
- 各タスクを控えへ移す前に、項目内の `Gate:` を実行し、コマンド・結果・日付を
  `docs/tasks_archive.md` の同じタスクIDへ1件で残す。
- 検査器を作るタスクは、正常系だけでなく**意図的に壊した入力で終了コード1**も確認する。
- `INFORMATIONAL`、既知の未接続、手動確認待ちは完了ではない。別の未完了タスクへ
  ID付きで分離した場合だけ、元タスクを閉じてよい。
- 見た目は代表画面を保存して目視、音は `verify_audio.py`、数値は `balance.gd`、
  決定性は `test_core.gd` を通す。詳細は `docs/task_completion_audit.md`。

## 進み具合

**済んだタスク 23 件 / 残り 10 件（ID の総数 33）。**

終わった項目はここから消して `docs/tasks_archive.md` へ移すので、
このファイルに `[x]` は残らない。**動いているかどうかは下の「直近に済んだこと」で見る。**
理由まで読みたいときは控えのほうを開く。

### 直近に済んだこと

- **[棚卸再開 D-7] UIラベルを `Terms` 経由へ寄せる。**
- **[Codex追記 H-2] 職業の絵が似すぎている組を、実画面の輪郭で分ける。**
- **[Codex追記 H-1] 町の広場と主街路を、町専用の低密度な舗装へ分ける。**
- **B-4. `docs/character_art_direction.md` を規約へ取り込む。**
- **A-6. 結末を戦記へ、過去反響を最後に。**

## 次にやる（効き目の大きい順）

### Codex の新規素材（独立して着手できる単位に分けた）

Codex は**データと設計だけ**を出している（`data/cross_world_arcs.json` は在るが
`src/quest/cross_world_arc_catalog.gd` は無い）。実装はこちら側。
順番は `docs/cross_world_story_design.md` の「導入順」に従う ―― **最初から
十二型を本体へつながない。** 永続セーブを使う物語なので、一型で旧セーブ・全滅・
途中終了・再開・決着の全経路を通してからカタログを広げる。

### Codex追記（2026-07-31 再プレイの改善アイテム）

以下は Codex が150秒の入力駆動プレイ（124コマ、20戦、危険度10到達）を
目視して追加した。根拠と完了条件は `docs/art_integration_audit.md` の
「2026-07-31 再プレイ」に置く。

### 棚卸再開（2026-07-31）

### 成長報酬の再設計（Codex追記 2026-07-31）

- [ ] **[Codex追記 E-1] 7種のアップグレードを「数値」から「出撃時の選択」へ直す。**
      `docs/progression_reward_design.md` の準備配分・支給品・店の重点・封の情報・
      任意使用の命綱を実装し、品数と値引き、ゴールドと薬草の役割重複をなくす。
      **Gate:** 各段階で実行時の選択が最低1つ増え、説明と段階差が一致する。

- [ ] **[Codex追記 E-2] 職業★6に装着制の固有報酬「継承印」を追加する。**
      15職を固有のルール変更で描き分け、出撃メンバーが解放した印から基本1枠、
      最大2枠だけ持ち込む。恒久能力値の加算と全効果の常時発動は行わない。
      ★6は構成が開く節目、★8は職業マスターとして役割を分ける。
      **Gate:** ★6未到達では選べず、解放・選択がセーブと `end_run()` をまたいで残り、
      同じシード・印・操作で戦闘、戦利品、行動順が一致する。

- [ ] **[Codex追記 E-3] 成長報酬の価値を画面と実測で確認できるようにする。**
      拠点で次段階の新しい選択、職業画面で★6の印と★7・8奥義、
      出撃前に選択枠を表示する。
      D-2完了後、未強化・中盤・全強化を同じ500シードで比較する。
      **Gate:** 対象となる成長報酬すべてに到達・発動テストがあり、未強化でも完走可能、単一報酬が
      常時最適にならず、`test_core.gd` と実プレイ相当 `balance.gd` が通る。

### 職業・特技・魔法の再設計（Codex追記 2026-07-31）

設計の詳細は `docs/job_ability_progression_design.md`。現状は15職×4段階の
60習得枠に対して固有技IDが37個しかなく、23枠が別職と重複する。共有技の多くも
威力・MP・対象・CTBの数値違いで、転職後は全習得技が常時並ぶため現職の個性が薄れる。

参考にするのは他社作品の名称や数値ではなく、次の構造だけ。

- FF5型: 現職の固有能力を土台に、過去職から選んだ能力だけを組み合わせる。
- DQ6型: 熟練と習得物は転職後も残り、複数の基本職が上位職への進路になる。
- 本作では「全習得技を常時使用」は採らず、長期成長と装着制を組み合わせる。
- 職業段階は4から8へ変更。★4を上位職、★6を継承構成、★7・8を奥義の節目にする。

新しい8段階は、★1=基本技、★2=用途の違う第2技、★3=現職固有特性、
★4=第3技＋上位職条件、★5=条件付き象徴技、★6=技の派生＋継承印、
★7=戦況転換技、★8=マスター奥義＋マスター表示とする。
段階を増やすだけの空報酬、能力値の恒久加算、弱い敵の反復稼ぎは入れない。

- [ ] **[Codex追記 F-1] 全40技を「役割・条件・代償・対抗策」で棚卸しする。**
      各技に、通常攻撃より選ぶ状況、支払うMP・CTB・HP・道具、連打が最適に
      ならない理由、所属職である理由を定義する。説明できない数値違いは統合し、
      上位技は別IDの上位互換ではなく、既存技の置換または派生選択にする。
      **Gate:** 同一の役割・発動条件・対象・代償しか持たない技を検査器が列挙し、
      allowlistには共有する設計理由と利用職を必須にする。違反fixtureで終了コード1。

- [ ] **[Codex追記 F-2] 名前と実効果が一致しない技を先に修正する。**
      `つらぬき` は実際に防御を抜き、`ときとまれ` は通常の全体鈍足と分離する。
      `ひっさつ` は瀕死等の成立条件を持たせ、`けむりだま` は単なる鈍足をやめる。
      `あそぶ` と `きまぐれ` は同じ乱数表の単体版・範囲版にせず、後者は
      `DetRng` で引いた複数候補から選ぶなど、操作上も別の技にする。
      **Gate:** 5組を修正前に失敗するfixtureで固定し、説明文・戦闘ログ・結果が一致する。

- [ ] **[Codex追記 F-3] 全15職を8段階へ移行し、既存セーブを壊さない。**
      旧★1〜4の必要熟練値を新★1〜4の基準にし、既存の熟練点と習得技は一切失わない。
      旧上限到達者は新★4から★5〜8を続けられる。既に就いた上位職は永久解放として
      保存し直し、新しい★4条件を満たさなくても再び封鎖しない。新規データでは
      対応する基本職2つの★4を上位職条件、★6を継承印、★8を職業マスターの条件にする。
      **Gate:** 旧セーブfixtureを読み、熟練点・習得技・経験済み上位職が減らず、
      新規セーブでは★4・6・7・8の各境界と解放が正しく保存・復元される。

- [ ] **[Codex追記 F-4] 「現職の技＋過去職から継承2枠」の構成へ変更する。**
      現職で習得済みの技はすべて使え、過去職の技は所有したまま出撃前に2つだけ選ぶ。
      `こうげき` と `ぼうぎょ` は常設。転職で無効になった構成は技ID順に勝手に
      詰めず、理由を表示して再選択させる。空欄のまま出撃することも認める。
      現職技だけで職業の役割が成立し、継承は欠点を消すものではなく戦術を足すものにする。
      **Gate:** 未習得技・同一技重複・3枠目を拒否し、セーブと `end_run()` 後も構成が残る。
      同じシード、現職、継承枠、入力列から行動順・ダメージ・戦利品が一致する。

- [ ] **[Codex追記 F-5] 15職と4属性へ固有の戦術語を割り当てる。**
      物理技は速攻・大技・貫通・連撃・状態追撃・中断・庇護・副次目的へ分ける。
      炎は重い瞬間火力と波及、氷はCTB遅延、雷は機械と予告技の中断、闇は
      HP・MP・次手番を交換する効果にする。属性は弱点倍率だけで終わらせない。
      戦士=大技と反撃、僧侶=治療順、盗賊=時間と戦利品、銃士=弾種、時術師=行動順、
      魔獣使い=撃破以外の決着など、15職すべての「毎手番の問い」をデータへ記述する。
      共有する基礎技は★1〜2まで。★5の象徴技と★7・8の奥義は全職固有にして別職へ配らない。
      **Gate:** 各職に現職だけの戦術fixtureと継承2枠の組合せfixtureを最低1件ずつ持ち、
      4属性は弱点なしの相手にも異なる結果を生む。乱数は `DetRng` だけを使う。

- [ ] **[Codex追記 F-6] ★7・8へ全15職ぶんの戦況転換技30種を実装する。**
      `docs/job_ability_progression_design.md` の奥義表を基準に、行動回数、対象再配分、
      行動表、資源変換、撃破以外の決着のいずれかを変える。単純な高倍率技で枠を埋めない。
      複合行動には `uses_per_battle`、二段階対象選択、残り打撃の再配分、装填・素材、
      再演禁止の共通契約を持たせ、個別Viewの特例で実装しない。
      **Gate:** 全30技に発動成功、条件不足、1戦制限、対象消滅、途中決着のfixtureを置く。
      奥義から奥義を再演できず、同じシード・対象選択から標的、ダメージ、行動順が一致する。

- [ ] **[Codex追記 F-7] 新しい職業進行と全技をUI・オート・実測へ接続する。**
      職業画面に8段階の全報酬、次の必要熟練、★4から開く進路、★6の継承印、
      ★7・8の奥義と使用制限を表示する。
      出撃前に現職技と継承2枠を比較でき、戦闘中は条件未成立・対象不在・資源不足の
      理由を表示する。オートは威力÷CTBだけでなく、防御、敵予告、状態、人数、
      回復余力、副次目的を評価し、適切なfixtureでは各役割を選べるようにする。
      **Gate:** `--shot=job`、`--shot=commands`、継承選択画面を目視し、全技の到達・発動・
      不成立テストを通す。D-2完了後に旧構成と新構成を同じ500シードで比較し、
      単一技・単一継承・単一職が常時最適なら完了にしない。

---

済んだものは `[x]` を付けてから `docs/tasks_archive.md` へ移す。
行数の上限は置かない。未実装の項目を整理目的で消してはならない。
