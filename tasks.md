# やること

思いついた改善とリファクタリングはここに書き溜めて、上から順に処理する。
やり終えたら行を消し、必要なら「済んだこと」に理由だけ残す。

判断の根拠は次に置いてある。着手前に読む。

- 戦闘の設計 … `docs/battle_design.md`
- 潜る理由（前提） … `docs/premise.md`
- 作業の規約 … `AGENTS.md`

---

## 進み具合

**済んだこと 70 件 / 残り 7 件。**

終わった項目はここから消して `docs/tasks_archive.md` へ移すので、
このファイルに `[x]` は残らない。**動いているかどうかは下の「直近に済んだこと」で見る。**
理由まで読みたいときは控えのほうを開く。

### 直近に済んだこと

- **引き離すときは床と壁の両方を動かす。** 壁だけ暗くしたら、草原が夜の洞窟に
- **マップチップは却下せずに直す形へ変えた。** 床と壁の RGB 距離が 40 未満の
- **私が承認されていない概念を設計根拠にしていた。** 「代償」「失うことが主題」は
- **生物相に意味を持たせる。** 敵の抽選を絞る（雪原は氷に強いもの、火山は火に
- **生物相は「その地帯の平均の危険度」で決める。** 種の危険度で決めたら、

## 次にやる（効き目の大きい順）

- [ ] **戦闘の UI がまだファミコンに見える。狙いは後期 SFC（DQ6 の透過処理）。**
      窓の不透明度（`PixelUI.WINDOW_ALPHA` 0.72）は入れたが、**それだけでは足りない。**
      後期 SFC が richer に見えたのは、半透明に加えて
      (1) 窓の中に上下の階調（単色の板にしない）
      (2) 文字と窓枠の落ち影
      (3) 枠の面取り（内側に明線、外側に暗線）
      (4) HP/MP バーの階調と、減ったぶんの残像
      (5) 背景を単色で塗らず、地平の階調と敵の足元の影
      を全部やっていたから。`src/ui/pixel_ui.gd` と `src/scenes/battle_view.gd`。
      **撮って見るまで「できた」と言わない。**
- [ ] **町の中が無い。NPC も居ない。** いま町は踏むと品書きが開くだけで、
      場所として存在していない。町の中の地図（宿・店・人）を作り、
      `ExploreView` でそのまま歩けるようにする（地図の型は `FieldMap` で揃っている）。
      NPC の話は封の在り処のヒントになるので、`docs/quest_design.md` の
      「町で聞ける」とここで繋がる。

- [ ] **クエストの骨格を入れる（封 3 つ）。** 設計は `docs/quest_design.md`。
      **まず AI 無しで**、テンプレート語彙表 × `DetRng` だけで成立させる。
      城の扉は封が 3 つ解けるまで開かない。封は危険度の帯（低・中・高）の洞の底。
      これで洞が「宝箱しかない寄り道」から「必須だが順番は自由」になる。
- [ ] **クエスト文の検算層（`quest_text.gd`）。** AI に書かせる前に作る。
      文字数・禁止語（他社固有名詞）・数値の混入・重複・必須キーの欠落を落とし、
      落ちた項目だけテンプレートへ差し替える。**単体テストを先に書く。**
- [ ] **ローカル AI をクエスト文に繋ぐ。** `ChronicleAI` を一般化して窓口を 1 本にする。
      構造を先に確定させ、届いたら表示だけ差し替える（待たせない・落ちても遊べる）。
- [ ] **町の聞き込み。** 封の在り処を町で聞ける。町に寄る理由がこれで生まれる。

- [ ] **マップチップを描き直す（床と壁の色を RGB 距離 40 以上 離す）。**
      寸法・色数・アルファは 5 種すべて規約内。落ちているのは床と壁の見分けだけ。
      生成器が測って見送るので、離した版を置けば自動で入る。

      | 候補 | 床と壁の距離 | 判定 |
      |---|---:|---|
      | 雪原 | 138.4 | 通る |
      | いまの ASCII タイル（読めている基準） | 65.9 | 通る |
      | 地下 | 9.3 | 落ちる |
      | 草原 | 9.0 | 落ちる |
      | 火山 | 7.5 | 落ちる |
      | 湿地 | 3.3 | 落ちる |

      生成器側で床を暖色へ寄せる補正も試したが、地下で 39.0（あと 1.0）にしか
      届かず、これ以上動かすと絵が壊れる。**描く側で離すのが正しい。**
      元の版が読めていたのは「茶色の床 + 紺の壁」で、明度ではなく色相が違ったから。

---

済んだものは `docs/tasks_archive.md` へ移す（このファイルは 200 行以内に保つ）。
