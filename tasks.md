# やること

思いついた改善とリファクタリングはここに書き溜めて、上から順に処理する。
やり終えたら行を消し、必要なら「済んだこと」に理由だけ残す。

判断の根拠は次に置いてある。着手前に読む。

- 戦闘の設計 … `docs/battle_design.md`
- 作業の規約 … `AGENTS.md`

---

## 次にやる（効き目の大きい順）

- [ ] **敵のドット絵を味方と同じ水準に上げる。** 味方だけ差し替えたので、
      いま敵が浮いている。`docs/chara_image/candidate_hero_*.png` と同じ経路で
      48px の敵シートを作り、`tools/gen_assets.py` の取り込み口に通す。
- [ ] **敵 AI が弱点も状態異常も使い分けない。** 「使える技から等確率で選ぶ」だけなので、
      深層の敵ほど的外れな手を打つ。属性の相性と残 HP を見る程度に上げる。
- [ ] **装備が出店にしか出ない。** 宝箱から装備が出れば、道中の宝箱を開ける理由が増える。
- [ ] **控えメンバーと編成。** 名簿が 4 人固定なので、拠点の「出撃する」が選択になっていない。
- [ ] **逃走の成功率が画面から読めない。** 素早さ差で決まるのに、選ぶ前に手がかりが無い。
- [ ] **戦記をローカル AI に繋ぐ。** 接続点は `src/game/chronicle.gd` の 1 か所に絞ってある。
      `facts_for_llm()` が渡すのは事実の構造だけ。失敗してもテンプレート出力に落ちること。

## リファクタリング

- [ ] **`src/scenes/battle_view.gd` が 700 行を超えた。** コマンド階層 / 描画 /
      オート戦闘の 3 つに分けられる。まずオート戦闘を別ファイルへ。
- [ ] **一覧の描画が 5 画面で個別実装。** カーソル + 行 + 右寄せの値、という同じ形を
      拠点・出店・戦闘・メニュー・装備で別々に書いている。共通部品にする。
- [ ] **`_notify` と通知タイマーが 3 画面で重複。** 窓の描画だけ `PixelUI.draw_notice()`
      に寄せたが、状態（文字列とタイマー）は各画面が持ったまま。
- [ ] **セーブの移行テストが無い。** `version` を上げたときに古いセーブが読めるかを
      `tests/test_core.gd` で検査する（いまは version 1 → 2 の分岐が無検査）。

## 演出・音

- [ ] **属性ごとの効果音が無い。** 炎・氷・雷が同じ「魔法」の音で鳴る。
- [ ] **状態異常に音が無い。** 眠り・毒は文字だけで、効いた手応えが薄い。
- [ ] **ダメージの数字が出ない。** メッセージ窓の文字だけなので、どこに効いたか分かりにくい。

## 自動プレイ（`--play`）

- [ ] **買い物と装備を実際に試さない。** 入力方針がランダム寄りなので、出店で買う・
      装備を付け替えるところまで踏まない。「買えるなら買う」程度の方針を足す。
- [ ] **勝てないと先に進めない。** いまは全滅すると拠点に戻ってやり直すだけ。
      到達階と勝敗を集計して、走らせるたびに数字で比べられるようにする。

---

## 済んだこと（理由だけ残す）

- 呪文名を他社の固有名詞から独自の造語へ。体系は借りてよいが語は借りない。
- 遭遇率を下げた。自動プレイで遊んだ時間の 54% が戦闘だったため。
- 戦記の行を折り返した。覚えた技を全部並べるので長さが読めず、窓から溢れていた。
- `DungeonMap.route()` を足した。酔歩では 3 分遊んでも 1 階から出られなかった。
- 入力をイベント判定に統一した。`_unhandled_input` の中で
  `Input.is_action_just_pressed()` を見ると、1 フレームに 2 つ届いた入力を取りこぼす。
- 味方のドット絵を外で描いたシートに差し替えた。ASCII マップでは後期 SFC の密度に
  届かなかった。取り込み口は生成器に置き、SFC の制約（72x128 / 15 色 / アルファ 2 値 /
  BGR555）を検算してから採用する。
