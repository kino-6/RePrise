# やること

思いついた改善とリファクタリングはここに書き溜めて、上から順に処理する。
やり終えたら行を消し、必要なら「済んだこと」に理由だけ残す。

判断の根拠は次に置いてある。着手前に読む。

- 戦闘の設計 … `docs/battle_design.md`
- 潜る理由（前提） … `docs/premise.md`
- 作業の規約 … `AGENTS.md`

---

## 進み具合

**済んだこと 126 件 / 残り 2 件。**

終わった項目はここから消して `docs/tasks_archive.md` へ移すので、
このファイルに `[x]` は残らない。**動いているかどうかは下の「直近に済んだこと」で見る。**
理由まで読みたいときは控えのほうを開く。

### 直近に済んだこと

- **はみ出しゲートが 12 件見つけ、そのうち 5 件は誤検出だった。** 窓が開く途中は
- **検算が空振りしていた。** 拍は座標ではなく `site_id`（"cave:2"）で土地に
- **窓は増やさない。** イベントと同じ `EventView` を使い回した。出すものは同じ
- **物語の拍は見送れない。** イベントは「立ち去る」が一手だが、拍を飛ばせると
- **順番は 1 本道に保つ。** 3 拍目の土地へ先に着いても何も起きない。

## 次にやる（効き目の大きい順）

- [ ] **造語と固有名詞を外部ファイルで差し替えられるようにする。**
      いま「奈落」「銀の砦」「資源」「危険度」「封」や、封の名の語彙表、町の名の
      語彙表、生物相の名などが `src/ui/terms.gd` / `src/game/lore.gd` /
      各生成器の定数に散っている。**`data/vocabulary.json` に集めて読み込む形にする。**
      見直しの対象なので、コードを触らずに差し替えられる状態にしておくのが先。
      `Terms` と `Lore` は残し、中身を JSON から読むだけにする（呼び出し側は無変更）。
      **AI に書かせる語彙（封の名など）も同じ表に置く**と、テンプレートと AI の
      出所が 1 か所になる。

- [ ] **イベントの深さ、残り 3 つ。** 選んだ結果は同じ窓で読ませるように
      したが、カタログにある `tags` と `fail_forward` はまだ使っていない。
      (1) 危険（risks）が実際に発火したときの分岐表示
      (2) 同じ世界で前に選んだことを覚えて、後のイベントの文へ反映
      (3) `WorldEventCatalog.facts_for_ai()` を `LocalAI` に繋いで表層を書かせる
---

済んだものは `docs/tasks_archive.md` へ移す（このファイルは 200 行以内に保つ）。
