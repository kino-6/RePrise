# やること

思いついた改善とリファクタリングはここに書き溜めて、上から順に処理する。
やり終えたら行を消し、必要なら「済んだこと」に理由だけ残す。

判断の根拠は次に置いてある。着手前に読む。

- 戦闘の設計 … `docs/battle_design.md`
- 潜る理由（前提） … `docs/premise.md`
- 作業の規約 … `AGENTS.md`

---

## 進み具合

**済んだこと 194 件 / 残り 17 件。**

終わった項目はここから消して `docs/tasks_archive.md` へ移すので、
このファイルに `[x]` は残らない。**動いているかどうかは下の「直近に済んだこと」で見る。**
理由まで読みたいときは控えのほうを開く。

### 直近に済んだこと

- **敵の絵は誤報だった。** `check_assets.py` が接頭辞で探していたが、敵の絵は
- **役ごとに言うことを変えた。** 全員が世間話をすると町が背景になる。
- **NPC を 6/17 → 17/17。** もとは 4 役を主人公の絵に割り当てていた。
- **`--shot=` がタイトルを撮っていたのは私が入れた行のせいだった。**
- **地図の外を先に塗る。** 町は 20〜30 タイルで画面（32）より狭く、余った側に

## 次にやる（効き目の大きい順）

- [ ] **遭遇時の閃光が強すぎる。往年の RPG のトランジションを調べて学ぶ。**
      いまは白を 2 回叩きつけているだけ（`_flash_into_battle`）で、目に刺さる。
      SFC 期は**画面そのものを変形させて**戦闘へ移っていた ―― 渦巻き、格子状の
      分割、上下からの走査線、モザイク、円形のワイプ、色の反転など。
      `docs/battle_design.md` と同じ手順で、**先に調べて docs にまとめ、
      それから実装する**（何が緊張を作っているのかを言語化してから触る）。
      Codex が `candidate_transition_*.png` を用意しているので、それも見る。
      **強い明滅は入れない**（読みづらいだけでなく、身体に障る人がいる）。

- [ ] **敵 6 体の 1 戦がまだ最長 17 秒。** オート中の 3 つの遅延（待ち 0.10 /
      点滅 0.12 / 閃光 0.18）はもう詰めた。平均は 7〜9 秒で詰まり検出には
      かからないが、群れの回だけ長い。残る手は**手番そのものを減らす**方向
      （群れの HP をもう少し下げる、範囲技の下限 76% を上げる）で、これは
      難度に直に効くので `balance.gd` で測りながらやる。**遅延をこれ以上
      削るのは筋が悪い**（0 にすると何が起きたか読めなくなる）。
      **Codex追記（2026-07-31）:** 150秒の再プレイでは、危険度6の戦闘が
      111.4秒から134.9秒まで23.5秒続いた。撮影時点では4体表示だったため、
      「6体戦だけ」の問題とは決めず、編成・HP・行動回数をログで分けて測る。

- [ ] **職業の絵が似すぎている組が残っている（描く側の作業）。**
      `gen_assets.py` が測って出す。いまの実測（基準 26）:
      **ninja–thief 2.6**、ranger–soldier 4.9、chronomancer–jester 6.3、
      paladin–spellblade 6.9、ほか。Codex が 6 職を描き直したが
      **ninja–thief は変わっていない**（chronomancer–jester が新たに近づいた）。
      原本は `docs/chara_image/candidate_hero_*.png`。**色ではなくシルエットで**
      分ける（`docs/character_art_direction.md` の基準に沿う）。

### Codex の新規素材（独立して着手できる単位に分けた）

Codex は**データと設計だけ**を出している（`data/cross_world_arcs.json` は在るが
`src/quest/cross_world_arc_catalog.gd` は無い）。実装はこちら側。
順番は `docs/cross_world_story_design.md` の「導入順」に従う ―― **最初から
十二型を本体へつながない。** 永続セーブを使う物語なので、一型で旧セーブ・全滅・
途中終了・再開・決着の全経路を通してからカタログを広げる。

- [ ] **A-4. 配置を接続する。** 町・洞・主戦前の 3 か所へ現在段階を重ねる。
      `WorldGenerator` と `verify()`（置いた段階に歩けるか）。
- [ ] **A-5. 十二型を解放する。** 同じ型が連続しないことを多数シードで検査。
      A-1〜A-4 が緑になってから。
- [ ] **A-6. 結末を戦記へ、過去反響を最後に。** `chronicle.gd` に 1 行足し、
      `world_that_did_not_close` の反響は**いちばん最後**に繋ぐ。

- [ ] **B-2. 戦闘エフェクト 12 種を取り込む。** `candidate_fx_*.png`（斬・突・炎・氷・
      雷・毒・眠・強化・弱化・爆・銃・回復）。いまは点滅と数字だけなので、
      技の系統ごとに絵を出す。`battle_view.gd` の被弾表現に足す。
      **オート中は短くする**（詰めた 3 つの遅延と同じ扱いにする）。
- [ ] **B-3. 画面トランジション 4 種を取り込む。** 仕様は同文書の「画面
      トランジション 4 種」。いまは暗転 1 種だけなので、場面ごとに使い分ける。
- [ ] **B-4. `docs/character_art_direction.md` を規約へ取り込む。**
      「職業名から連想した定番記号を自動で足さない」という基準が書かれている。
      `AGENTS.md` の文字とキャラの規約に要点を写し、`HERO_LOOKS` の指針も揃える。

### Codex追記（2026-07-31 再プレイの改善アイテム）

以下は Codex が150秒の入力駆動プレイ（124コマ、20戦、危険度10到達）を
目視して追加した。根拠と完了条件は `docs/art_integration_audit.md` の
「2026-07-31 再プレイ」に置く。

- [ ] **[Codex追記 C-2] 自動プレイを町の中に滞在させ、監査値を正しくする。**
      今回の町滞在は108.9〜109.2秒の0.3秒だけで、NPCを一人も撮れなかった。
      町では最低2人に話し、宿か店を試してから出る経路を持たせる。
      併せて、危険度10へ到達したのに「最も危険: 1」と出る集計と、
      世界↔町の2状態反復を検出できない詰まり判定を直す。

- [ ] **[Codex追記 C-3] 戦闘の敵を整数倍で見せる。**
      48x48・64x64を512x320へ等倍表示しているため、帝国兵の顔と装備、
      ボスの格が消えている。最近傍2倍を基準に、1〜6体の配置、影、カーソル、
      状態表示、ダメージ数字を表示矩形から計算し直す。半端な倍率は使わない。

- [ ] **[Codex追記 C-4] 戦闘背景6種を生物相へ接続する。**
      現在は紺の空と地面の階調だけで、追加敵が広い空白に浮いて見える。
      `candidate_battle_bg_*` を生成器から `assets/backgrounds/` へ出し、
      草原・湿地・雪原・火山・地下・帝国工廠へ割り当てる。
      画像欠落時は現在の階調背景へ戻す。

- [ ] **[Codex追記 C-5] 職業画像を拠点と職業選択で鑑賞できる大きさにする。**
      現在の48x64表示では角、仮面、有機襟がほぼ読めない。
      72x96の最近傍3倍を基準に人物領域を確保し、全15職を同じ枠で撮る。
      （`--shot=` が暗転待ちの競合でタイトルを撮る問題は解決済み）

- [ ] **[Codex追記 C-6] イベントFX 4種を重要場面へ接続する。**
      `assets/effects/event_*.png` は生成済みだが実行時参照がない。
      世界門、封印解除、年代記、帝国警報へ固定対応させる。
      演出失敗時は現在の進行へ戻し、乱数・セーブ・イベント結果を変えない。

- [ ] **[Codex追記 C-8] 町の設定を先に決め、間取り・施設・NPCへ一貫して反映する。**
      現在は「中央広場から宿・店・出口が見える」とコメントしているのに、
      実装は長方形へ建物2棟と石畳を個別に乱数配置するだけで、広場も町固有の核もない。
      生業・支配者・現在の問題・目印を持つ `TownProfile` を先に `DetRng` で選び、
      街路、建物、装飾、NPCの役と台詞を同じProfileから作る。同一世界の4町は
      核を重複させず、100シードで決定性、全施設への到達性、入口からの案内性を検査する。

- [ ] **[Codex追記 C-9] 装備入手直後に「今そうびする」を選べるようにする。**
      宝箱・イベント・店の全入手経路を一つの通知へ集約し、
      「今そうび／もちものへ」を出す。今そうびでは装備可能な仲間と、
      現在値→装備後の能力差、外れる装備を表示する。キャンセル時も入手品は失わず、
      表示語は `Terms` に置き、選択中に別の入力や遭遇が割り込まないようにする。

- [ ] **[Codex追記 C-10] 「さいきょう装備」を共通処理として実行できるようにする。**
      装備画面から全員へ実行でき、`AutoPlay` も開始時と装備入手後に同じ処理を呼ぶ。
      単純な能力合計ではなく、職業ごとの物理・魔法・速度・耐久と装備効果の重みを
      明示し、手持ち1個を複数人へ配らない。同点は仲間ID・装備ID順で固定して
      乱数を使わず、代表職と複数個所持の割当をテストする。

---

済んだものは `docs/tasks_archive.md` へ移す（このファイルは 200 行以内に保つ）。
