# やること

思いついた改善とリファクタリングはここに書き溜めて、上から順に処理する。
やり終えたら行を消し、必要なら「済んだこと」に理由だけ残す。

判断の根拠は次に置いてある。着手前に読む。

- 戦闘の設計 … `docs/battle_design.md`
- 潜る理由（前提） … `docs/premise.md`
- 作業の規約 … `AGENTS.md`

---

## 進み具合

**済んだこと 105 件 / 残り 3 件。**

終わった項目はここから消して `docs/tasks_archive.md` へ移すので、
このファイルに `[x]` は残らない。**動いているかどうかは下の「直近に済んだこと」で見る。**
理由まで読みたいときは控えのほうを開く。

### 直近に済んだこと

- 撮影の近道が `GameState.world_pos` を当てにしていて空振りした。
- **イベントが残す効果はすべてラン内資源に置いた**（遭遇率・品数・一時的な力）。
- **`verify()` に「置いたイベントに歩いて行けるか」を足した。** 届かないイベントは
- **一覧の 1 行は枠に収まる長さで切る**（`PixelUI.clip`）。全部並べたら溢れた。
- **必ず立ち去れる。** キャンセルで見送れて、踏み直せばまた開く。

## 次にやる（効き目の大きい順）

- [ ] **検算層が 2 本ある。** `QuestText`（封の名）と `QuestNarrativeText`
      （イベントの表層）が、禁止語・数字・記号の表をそれぞれ持っている。
      片方だけ直す事故が起きるので 1 本へ寄せる。
- [ ] **イベントの効果 11 個が文だけで、まだ効かない。**
      `EventEffects.INFORMATIONAL` に明示してある（黙って無視ではない）。
      世界地図への書き込み（近道・道を失う・地図が分かる）、主戦の下準備
      （手が読める・弱る）、5 人目の仲間、宿の格上げ。**ここを空にするのが次。**
- [ ] **イベントに AI の表層を繋ぐ。** 骨格と `DetRng` の表層までは動いている。
      `WorldEventCatalog.facts_for_ai()` と `apply_ai_skin()` が既にあるので、
      `LocalAI` から呼んで `QuestNarrativeText` に通すだけ。封の名と同じ作り。

---

済んだものは `docs/tasks_archive.md` へ移す（このファイルは 200 行以内に保つ）。
