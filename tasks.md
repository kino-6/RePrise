# やること

思いついた改善とリファクタリングはここに書き溜めて、上から順に処理する。
やり終えたら行を消し、必要なら「済んだこと」に理由だけ残す。

判断の根拠は次に置いてある。着手前に読む。

- 戦闘の設計 … `docs/battle_design.md`
- 潜る理由（前提） … `docs/premise.md`
- 作業の規約 … `AGENTS.md`

---

## 進み具合

**済んだこと 97 件 / 残り 2 件。**

終わった項目はここから消して `docs/tasks_archive.md` へ移すので、
このファイルに `[x]` は残らない。**動いているかどうかは下の「直近に済んだこと」で見る。**
理由まで読みたいときは控えのほうを開く。

### 直近に済んだこと

- **待たせない。** 世界はテンプレートの名前で完成済みで、数秒後に届いたら
- **実機で通した。** Ollama から JSON が返り、検算が 6 項目すべて採用、
- **HTTP の接続点を 1 つにした。** `LocalAI` だけが Ollama を叩き、戦記も
- **AI は構造に触らない**ことをテストで固定した。名と一文を当てたあとも
- **項目ごとに落とす。** 1 つ変でも全部捨てない。封の名が 1 つ駄目でも残り 2 つは

## 次にやる（効き目の大きい順）

- [ ] **Codex のイベントカタログを本体に繋ぐ。** 32 型のサブクエストが
      `data/world_events.json` / `src/quest/world_event_catalog.gd` /
      `src/quest/quest_generator.gd` に揃っていて、単体では 25 項目緑。
      **だがゲームからは一度も呼ばれていない**（scenes/ から参照ゼロ）。
      設計はこちらと噛み合う（構造はゲーム、`DetRng` が表層、AI は 4 項目だけ）。
      繋ぐのは 3 か所:
      (1) `WorldGenerator` で街道 1 / 町 1 / 洞 1 を世界へ置く
      (2) 踏んだときに選択肢を出す画面（`ShopView` の作りが近い）
      (3) 効果トークンを実数へ変換する解決表を 1 か所に作る
          （JSON に HP 量や金額を書かない、という Codex の分離を守る）
      **`verify()` に「置いたイベントに歩いて行けるか」を足すこと。**
- [ ] **検算層が 2 本ある。** `QuestText`（封の名）と `QuestNarrativeText`
      （イベントの表層）が、禁止語・数字・記号の表をそれぞれ持っている。
      片方だけ直す事故が起きるので、繋ぐときに 1 本へ寄せる。

---

済んだものは `docs/tasks_archive.md` へ移す（このファイルは 200 行以内に保つ）。
