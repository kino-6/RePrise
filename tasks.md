# やること

思いついた改善とリファクタリングはここに書き溜めて、上から順に処理する。
やり終えたら行を消し、必要なら「済んだこと」に理由だけ残す。

判断の根拠は次に置いてある。着手前に読む。

- 戦闘の設計 … `docs/battle_design.md`
- 潜る理由（前提） … `docs/premise.md`
- 作業の規約 … `AGENTS.md`

---

## 完了Gate

- **コードや画像が「ある」だけでは完了にしない。** 実行時に通る経路と結果を見る。
- 各タスクを控えへ移す前に、項目内の `Gate:` を実行し、コマンド・結果・日付を
  `docs/tasks_archive.md` の同じタスクIDへ1件で残す。
- 検査器を作るタスクは、正常系だけでなく**意図的に壊した入力で終了コード1**も確認する。
- `INFORMATIONAL`、既知の未接続、手動確認待ちは完了ではない。別の未完了タスクへ
  ID付きで分離した場合だけ、元タスクを閉じてよい。
- 見た目は代表画面を保存して目視、音は `verify_audio.py`、数値は `balance.gd`、
  決定性は `test_core.gd` を通す。詳細は `docs/task_completion_audit.md`。

## 進み具合

**済んだタスク 39 件 / 残り 3 件（ID の総数 42）。**

終わった項目はここから消して `docs/tasks_archive.md` へ移すので、
このファイルに `[x]` は残らない。**動いているかどうかは下の「直近に済んだこと」で見る。**
理由まで読みたいときは控えのほうを開く。

### 直近に済んだこと

- **[Codex追記 P-5] 自動プレイの戦闘ログは、戦闘開始時の場所と危険度を結果画面まで保持する。**
- **[Codex追記 P-6] タスク台帳で枝番IDも1件として検査する。**
- **[Codex追記 E-1] 7種のアップグレードを「数値」から「出撃時の選択」へ直す。**
- **[Codex追記 P-3] 実入力プレイを深層・報酬直後・失敗操作へ狙って通せるようにする。**
- **[Codex追記 P-2] 自動プレイのコマを実行単位で隔離し、古い画面を混ぜない。**

## 次にやる（効き目の大きい順）

### Codex の新規素材（独立して着手できる単位に分けた）

Codex は**データと設計だけ**を出している（`data/cross_world_arcs.json` は在るが
`src/quest/cross_world_arc_catalog.gd` は無い）。実装はこちら側。
順番は `docs/cross_world_story_design.md` の「導入順」に従う ―― **最初から
十二型を本体へつながない。** 永続セーブを使う物語なので、一型で旧セーブ・全滅・
途中終了・再開・決着の全経路を通してからカタログを広げる。

### Codex追記（2026-07-31 再プレイの改善アイテム）

以下は Codex が150秒の入力駆動プレイ（124コマ、20戦、危険度10到達）を
目視して追加した。根拠と完了条件は `docs/art_integration_audit.md` の
「2026-07-31 再プレイ」に置く。

### 棚卸再開（2026-07-31）

### 実入力プレイ棚卸し（Codex追記 2026-07-31）

`playtest-game-build` の観点表に沿い、タイトル／序章／拠点／世界移動／町とNPC／
宿と4分類の店／出来事／通常戦闘／全滅と戦記／地図／道具・装備メニューを、
ゲーム内処理の直呼びではなく入力イベントだけで180秒確認した。
2回出撃、町8回（滞在83秒）、NPC会話16回、宿8回、店8回、戦闘8回、
通常遭遇間隔は最短8歩、戦闘は平均3.5秒・最長18.2秒、自然全滅から拠点へ復帰、
メニューの「とじる」は4/4回成功、25秒以上の貼り付きは0件だった。

機械Gateは `test_core.gd` 518/0、`test_window_system.gd` 19/0、
技・用語・文章・音・参照素材の検査を通過。40シードの全周方針は主撃破25%で
現行帯内。以下は、この通しで残った「動くが判断しにくい箇所」と、
同じ検査を再現可能にするための不足だけを記す。

### 実入力再監査（Codex追記 2026-07-31）

現行の作業ツリーを対象に、ユーザーセーブを退避して次の3経路を入力イベントだけで通した。

- `--play=150 --seed=73101`: 危険度10まで到達。町4回、宿4回、住人会話8回、
  店4分類、出来事3件、戦闘10回。通常遭遇は最短8歩、25秒以上の貼り付きは0件。
- `--play=90 --seed=73102 --dev-level=12 --dev-seals --dev-castle --dev-drop=war_axe`:
  装備提示から主戦・全滅結果まで6周。各周とも入力停止は0件。
- `--play=60 --seed=73103 --dev-level=8 --dev-floor=6`: 最初の操作可能状態が危険度6。
  町2回、宿2回、住人会話4回、店4分類、戦闘3回を通り、危険度9まで進行。

機械Gateは `test_core.gd` 538/0、`test_ultimates.gd` 247/0、
`check_ui.py` 34画面で違反0件。既存のE-1〜E-3は下記のとおり実残件だった。
それとは別に、プレイヤーから見える町の差、監査ログ、タスク台帳に3件の不足を確認した。

- [ ] **[Codex追記 P-4] TownProfile の違いを、台詞を読まなくても町の形から判別できるようにする。**
      `run_73101/008_EXPLORE.png`（危険度1）と `083_EXPLORE.png`（危険度8）は、
      色と人物は違っても、どちらも「中央5x5広場の左右に横長の宿・店を1棟ずつ置く」
      同じ輪郭に見える。実装も `_place_profile_facilities()` が左右2棟を固定しており、
      Profile差は中央の目印、散らした床、役と台詞へ寄っている。
      内部指紋50通りという既存Gateは座標差を検出できるが、**人が見て別の町と分かるか**
      は検出していない。

      入口の南中央固定と迷わせない主街路は残す。そのうえで、市場広場型、街道宿場型、
      工房街型、段丘・水辺型など、施設のまとまりと空地の使い方が異なる
      `layout_family` をProfileから決める。生業・支配・問題・目印のうち最低2要素を
      建物、露店、柵、作業場、祠などの画面上の形へ反映し、色替えだけにしない。

      **Gate:** 同一世界の4町を512x320で保存し、最低3種の大構成が画面だけで区別できる。
      同じ生物相どうしでも建物輪郭・広場位置・目印の組が重ならない。
      100町で南入口、到着余白、宿・店・出口への到達性、決定性を保ち、
      `test_core.gd` と60秒の実入力プレイを通す。

- [ ] **[Codex追記 E-2b] 継承印 15 種の効き目を実装する。**
      **装着の仕組みは済んでいる**（★6 で開く / 基本 1 枠・最大 2 枠 /
      未解放・重複・枠外を拒む / 外れたら詰めずに空ける / セーブに残る）。
      15 職ぶんの `rule` も `data/jobs.json` に書いてある。
      残っているのは**その `rule` が実際に効くこと**。

      案は `docs/progression_reward_design.md` の表。**常時発動にしない**
      （1 戦に 1 度、または開戦時に 1 度）。**能力値の加算にしない。**
      15 個ぶん別々の答えが要るので、**5 職ずつ 3 回**に分ける。

      **再監査:** `screen_inherit.png` では印の装着名まで表示され、選択・枠・保存の
      fixtureも通る。一方、戦闘・探索側に選択済み `inherit_signs` を読んで各 `rule` を
      発動する処理は無く、現行テストも印の定義と装着だけを見ている。
      「印を選べる」と「印が効く」を分けた現在の残件記載は正しい。

      **Gate:** 各印に発動 fixture（効いた / 条件を満たさない / 1 戦に 1 度）。
      同じシード・印・操作から戦闘・戦利品・行動順が一致する。

- [ ] **[Codex追記 E-3] 成長報酬の価値を画面と実測で確認できるようにする。**
      拠点で次段階の新しい選択、職業画面で★6の印と★7・8奥義、
      出撃前に選択枠を表示する。
      **再監査:** `screen_upgrade.png` は新しい説明を表示するが、
      `screen_depart.png` には支給品・店の重点の選択と出撃時サマリーが無い。
      `screen_inherit.png` も印名だけで、何が・いつ・1戦に何度効くかを比較できない。
      `--play=150` でも `STRONGHOLD → EXPLORE` の間に成長報酬の選択画面は現れなかった。
      E-1とE-2b完了後、未強化・中盤・全強化を同じ500シードで比較する。
      **Gate:** 対象となる成長報酬すべてに到達・発動テストがあり、未強化でも完走可能、単一報酬が
      常時最適にならず、`test_core.gd` と実プレイ相当 `balance.gd` が通る。

### 職業・特技・魔法の再設計（Codex追記 2026-07-31）

設計の詳細は `docs/job_ability_progression_design.md`。旧監査時の
「15職×4段階・固有技37個・23枠重複」は解消済みで、現状は15職×8段階、
技画像85/85、15職×2奥義まで実装され、F-7でUIとオートにも接続済み。
この系列で残る実装は上記E-2bの継承印15種の効き目と、E-3の表示・実測である。

参考にするのは他社作品の名称や数値ではなく、次の構造だけ。

- FF5型: 現職の固有能力を土台に、過去職から選んだ能力だけを組み合わせる。
- DQ6型: 熟練と習得物は転職後も残り、複数の基本職が上位職への進路になる。
- 本作では「全習得技を常時使用」は採らず、長期成長と装着制を組み合わせる。
- 職業段階は4から8へ変更。★4を上位職、★6を継承構成、★7・8を奥義の節目にする。

新しい8段階は、★1=基本技、★2=用途の違う第2技、★3=現職固有特性、
★4=第3技＋上位職条件、★5=条件付き象徴技、★6=技の派生＋継承印、
★7=戦況転換技、★8=マスター奥義＋マスター表示とする。
段階を増やすだけの空報酬、能力値の恒久加算、弱い敵の反復稼ぎは入れない。

---

済んだものは `[x]` を付けてから `docs/tasks_archive.md` へ移す。
行数の上限は置かない。未実装の項目を整理目的で消してはならない。
