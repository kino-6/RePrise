# やること

思いついた改善とリファクタリングはここに書き溜めて、上から順に処理する。
やり終えたら行を消し、必要なら「済んだこと」に理由だけ残す。

判断の根拠は次に置いてある。着手前に読む。

- 戦闘の設計 … `docs/battle_design.md`
- 作業の規約 … `AGENTS.md`

---

## 進み具合

**済んだこと 43 件 / 残り 8 件。**

終わった項目はここから消して `docs/tasks_archive.md` へ移すので、
このファイルに `[x]` は残らない。**動いているかどうかは下の「直近に済んだこと」で見る。**
理由まで読みたいときは控えのほうを開く。

### 直近に済んだこと

- **裸でダンジョンに潜っていた。** 職業ごとの初期装備を毎ラン支給する
- **オートが通常攻撃を連打しているだけに見える。** 実際「効率が最大の技」しか
- **オートの止め方が画面に無い。** 始め方だけ見えて止め方が見えないのは不親切。
- **宝箱と盗むから金しか出ない。** 確率が低すぎた（宝箱で物が出るのは 45%、
- **出店の品がほとんど買えない。** 稼ぎを測っていなかったのが原因。balance.gd に

## 次にやる（効き目の大きい順）

- [ ] **BGM のループが短い。** 戦闘は 8 小節（約 12 秒）で回るので、1 戦のあいだに
      何度も同じところへ戻る。拠点と道中も 16 小節。A→B→A の構成にして
      32 小節前後へ伸ばし、対旋律と抜き差しを入れる。`tools/gen_audio.py`。
- [ ] **ダンジョンに潜る理由が決まっていない（要・決定）。** 仕組みは「レベルと装備を
      失い、熟練だけ持ち帰る」だが、なぜ潜るのかを言葉にしていない。
      戦記（ローカル AI）に渡す事実も、拠点の文言も、主の位置づけもここで決まる。
      **作品の設定はユーザーの領分なので、こちらでは決めない。**

- [ ] **毒が戦闘をまたがない。** いま状態異常は戦闘の終わりに消える。
      DQ は毒を歩数で削るので、道具を使うか出店へ急ぐかの判断が道中に生まれる。
      消耗戦（HP と MP を持ち越す）という前提と噛み合うので、入れる価値がある。
- [ ] **MP の回復手段が「まほうのみず」しか無い。** 深い階ほど魔法職が置物になる。
      出店の品を増やすか、休息の値段を見直すか、拠点の強化に一段足すか。
- [ ] **設定が無い。** 音量も文字の速さも変えられない。
      自動プレイで音を切る仕組み（AudioServer のミュート）は既にあるので、
      それを画面から触れるようにする。
- [ ] **キーが固定。** Z / X / 方向キー（WASD）だけで、割り当てを変えられない。
- [ ] **セーブが 1 つ。** 拠点の状態しか持たないので壊れると戻せない。
      書き出す前に 1 世代だけ退避すれば、事故の大半は防げる。

- [ ] **マップチップを描き直す（床と壁の色を RGB 距離 40 以上 離す）。**
      寸法・色数・アルファは 5 種すべて規約内。落ちているのは床と壁の見分けだけ。
      生成器が測って見送るので、離した版を置けば自動で入る。

      | 候補 | 床と壁の距離 | 判定 |
      |---|---:|---|
      | 雪原 | 138.4 | 通る |
      | いまの ASCII タイル（読めている基準） | 65.9 | 通る |
      | 地下 | 9.3 | 落ちる |
      | 草原 | 9.0 | 落ちる |
      | 火山 | 7.5 | 落ちる |
      | 湿地 | 3.3 | 落ちる |

      生成器側で床を暖色へ寄せる補正も試したが、地下で 39.0（あと 1.0）にしか
      届かず、これ以上動かすと絵が壊れる。**描く側で離すのが正しい。**
      元の版が読めていたのは「茶色の床 + 紺の壁」で、明度ではなく色相が違ったから。

---

済んだものは `docs/tasks_archive.md` へ移す（このファイルは 200 行以内に保つ）。
