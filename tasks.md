# やること

思いついた改善とリファクタリングはここに書き溜めて、上から順に処理する。
やり終えたら行を消し、必要なら「済んだこと」に理由だけ残す。

判断の根拠は次に置いてある。着手前に読む。

- 戦闘の設計 … `docs/battle_design.md`
- 潜る理由（前提） … `docs/premise.md`
- 作業の規約 … `AGENTS.md`

---

## 完了Gate

- **コードや画像が「ある」だけでは完了にしない。** 実行時に通る経路と結果を見る。
- 各タスクを控えへ移す前に、項目内の `Gate:` を実行し、コマンド・結果・日付を
  `docs/tasks_archive.md` の同じタスクIDへ1件で残す。
- 検査器を作るタスクは、正常系だけでなく**意図的に壊した入力で終了コード1**も確認する。
- `INFORMATIONAL`、既知の未接続、手動確認待ちは完了ではない。別の未完了タスクへ
  ID付きで分離した場合だけ、元タスクを閉じてよい。
- 見た目は代表画面を保存して目視、音は `verify_audio.py`、数値は `balance.gd`、
  決定性は `test_core.gd` を通す。詳細は `docs/task_completion_audit.md`。

## 進み具合

**済んだタスク 69 件 / 残り 6 件（ID の総数 75）。**

終わった項目はここから消して `docs/tasks_archive.md` へ移すので、
このファイルに `[x]` は残らない。**動いているかどうかは下の「直近に済んだこと」で見る。**
理由まで読みたいときは控えのほうを開く。

### 直近に済んだこと

- **[S-8] 呼び名を漢字へ寄せた結果、12px で漢字を描いている場所を直す。**
- **[S-7] 同じ文が 2 か所にあって、片方だけが効いている状態を無くす。**
- **[S-1] `main.gd` から開発足場を出す。**
- **[Codex追記 V-3] ローカルAIの壊れたJSONを通常エラーにせず既定文へ戻す。**
- **[Codex追記 V-2] コア検算を領域分割し、重い世界生成を並列化する。**

## 次にやる

**S 系は「複数のエージェントが同時に触っても壊れない形」にするための分割。**
機能は増えない。だから **1 件ずつ、緑のまま終える**（まとめてやると、
壊したのが分割なのか元の不具合なのかが分からなくなる）。

前提として実測がある。直近 40 コミットで触られた回数と行数:

| ファイル | 行 | 40 中 |
|---|---:|---:|
| `tests/test_core.gd` | 3732 | 19 |
| `src/scenes/main.gd` | 3009 | 16 |
| `tasks.md` / `docs/tasks_archive.md` | – | 30 / 29 |
| `src/ui/terms.gd` | 599 | 8 |
| `data/build.json` | – | 39 |

**ファイル分割だけでは足りない。** 実害が出たのは git の衝突ではなく
**作業ツリーの共有**だった（Codex の書きかけを踏んで `test_core` が一度落ち、
再実行で緑になった）。分割は「同じ関数を二人で書き換える」を減らすもので、
中間状態は消えない。**別の worktree で作業する**のと両輪にする。

- [ ] **[S-1b] 開発用の計測カウンタと窓口も `DevProbe` へ寄せる。**
      S-1 で撮影・fixture・道案内（814 行）は出した。残っているのは
      `dev_mode_name` から `dev_chests_taken` までの**窓口 17 個（約 100 行）**で、
      これは**機能を足すたびに 1 つ増える**（R-3 で実際に 2 個増えた）。

      出せなかった理由は 1 つ。**数える場所が本編の中**にある
      （`_on_inn` / `_on_chest` / `_on_talked` などが `_dev_*` を直接足している）。
      窓口だけ移すと `DevProbe` が `main` の私有変数を読みに行く形になり、
      増やす場所は本編のままなので**衝突は減らない**。

      なので S-4 の境界作業と一緒にやる。Flow ノードへ割るときに
      「数えるのは誰か」を決め、`dev.note_inn()` のような 1 つの窓口へ集める。
      呼び出し側（`src/dev/autoplay.gd` の 37 か所）も同時に付け替える。

      **Gate:** `--play=120` の報告に出る数（宿・店・会話・仕事場・物資箱・
      宝箱・格上）が**移す前と 1 つも変わらない**こと。`test_core` 全 suite 緑。
      `main.gd` が 2100 行以下（S-4 と合わせて達成する）。

- [ ] **[S-2] `tests/test_core.gd` を領域ごとのファイルへ分ける。**
      3732 行、churn 最大。V-2 で `--suite` の並列実行までは入ったが、
      **表もテスト本体も 1 ファイルのまま**なので、二人が同じ末尾へ追記し続ける。

      `tests/core/<suite>.gd` の 8 本と、`_check` / `_equal` を持つ共通の基底へ。
      ランナー（`tools/test_core.py`）は既にあるので入口は変えない。
      **V-3で追加したAI JSON境界の3検査**（不正JSON／コードフェンス／本文中の波括弧）は
      `presentation` へ移し、分割時に落とさない。

      **Gate:** 分割前後で**成功件数が 1 件も減っていない**こと（数を並べて出す）。
      `tools/test_core.py` と `godot --headless --script tests/test_core.gd` の
      両方が同じ件数で緑。**わざと 1 件壊して終了コード 1** も確認する。

- [ ] **[S-3] `terms.gd` と `vocabulary.json` の追記位置を機能で決める。**
      いま全員が末尾へ足すので必ずぶつかる。**分割はしない** ――
      `Terms.X` の参照が全画面にあり、分けると呼び名が変わって
      「1 か所を書き換えれば表示が揃う」という取り決めのほうが壊れる。

      機能別の見出しで区切り、「戦闘の語は戦闘の節へ」を規約にするだけでよい。
      `data/vocabulary.json` も同じ順に並べ替える。

      **Gate:** `check_terms.py` が直書き 0 件、`check_ui.py` 47 画面、
      **語の中身が 1 つも変わっていない**こと（並べ替え前後で
      `vocabulary.json` の key/value 集合が完全一致）。

- [ ] **[S-4] `main.gd` の本体を Flow ノードへ分ける（4 段階）。**
      S-1 のあとに残る ~2000 行を責務ごとの子ノードへ移し、`main.gd` は
      **View 生成・Mode・遷移・signal 配線だけ**（~400 行）にする。

      | 順 | 出すもの | 行 |
      |---|---|---:|
      | S-4a | `EventFlow`（イベント／物語／格上報酬／task） | ~500 |
      | S-4b | `BattleGate`（戦闘の出入りと勝敗後の分岐） | ~240 |
      | S-4c | `RunFlow`（ラン開始・世界と洞の出入り・中断） | ~300 |
      | S-4d | `TownFlow`（町の中） | ~150 |

      **`_pending_*` の直列フロー（D-9）だけは 1 か所に残す。**
      装備提示 → 予約戦闘 → 欲の格上 → 探索、の順序は
      イベント側と戦闘側の両方から積まれるので、列を二つに割ると
      D-9 で直したはずの競合が別の形で戻る。ここが唯一の設計判断。
      S-4aではV-3の「AI返答が壊れた1件は既定文のまま送り、残りのイベント生成を続ける」
      経路も `EventFlow` へ移す。

      **Gate:** 1 段階ごとに `test_core` 全 suite ＋ `test_world_events` ＋
      `check_ui.py` を緑にしてから次へ進む。`--play=120` で詰まり 0 件。
      `balance.gd -- --runs=100` が帯の中（Flow を切ると遭遇と報酬の経路が
      通る場所が変わるので、数値でも確かめる）。

- [ ] **[S-5] 台帳と生成物を、毎コミット触らない形にする。**
      `data/build.json` は 40 コミット中 39 回変わるが、中身は
      `stamp_version.py` の生成物なので**全部ノイズ**。追跡をやめるか、
      書き出しの直前だけ更新する。

      `tasks.md` / `tasks_archive.md` は 30 / 29 回。1 タスク 1 ファイル
      （`docs/tasks/<ID>.md`）にすれば衝突は消えるが、**「上から順に取る」
      一覧性が落ちる**ので、こちらはユーザーの判断を仰いでから。
      勝手に運用を変えないこと。

      **Gate:** `tasks_status.py` が終了コード 0（ID の重複・移し忘れを見る）。
      `build.json` を追跡から外すなら、**書き出したビルドに版が刻まれている**
      ことを実際の exe で確認してから外す（刻印が消えるほうが困る）。

- [ ] **[S-6] 画面に出る文を、コードから `data/` へ出す。**
      不変条件 5（画面に出す言葉は `terms.gd` に置く）が、**見張られている
      範囲でしか守られていない。** `check_terms.py` が見るのは
      `src/scenes/` と `src/ui/` だけで、しかも「語彙に載っている語」の直書きしか
      落とさない。実測すると、コードへ直書きされた日本語の文字列は **39 ファイル
      1052 件**ある。

      | 件 | ファイル | 中身 |
      |---:|---|---|
      | 159 | `src/ui/terms.gd` | 語彙の既定値。**これは正しい**（受け皿） |
      | 146 | `src/battle/battle_system.gd` | **戦闘ログの本文**。ここが最大の穴 |
      |  78 | `src/quest/quest_text.gd` | AI 文の検算メッセージ |
      |  63 | `src/quest/event_effects.gd` | 効果の説明文 |
      |  56 | `src/game/lore.gd` | 前提の文 |
      |  39 | `src/scenes/main.gd` | 通知（「洞を ぬけた。」ほか） |

      **全部を外へ出すのではない。** 分けるのは 1 本の線だけ ――
      **画面に出るか、開発者だけが読むか。** `push_error` / `push_warning` /
      `print` の文言は残す。動かすのは、ログ窓・通知・イベント文のように
      **プレイヤーが読む文**。

      いちばん効くのは `battle_system.gd` の 146 件。「%sの こうげき！」の類が
      戦闘ロジックの中に散っているので、言い回しを直すたびに戦闘の中身を
      触ることになる（触ってはいけない場所を触る理由を作っている）。

      **一度に 1052 件を動かさない。** 差分が読めなくなるので領域ごとに切る:
      S-6a 戦闘ログ / S-6b イベント効果と物語 / S-6c 通知と検算メッセージ。

      **S-6a は実装済み**（118 種を `src/ui/battle_text.gd` と
      `data/vocabulary.json` の `battle` 節へ。`battle_system.gd` に残る日本語は
      `push_error` の 3 件だけ）。安全網は `tests/_log_dump.gd` に置いた ――
      決まった種で戦闘を回し、全技・全道具を名指しで 1 回ずつ通して、出た行を
      すべて並べる。移動の前後で **770 行が完全一致**することを確認した。
      `check_terms.py` に「外部化が済んだファイルへ直書きが戻っていないか」を
      足し、自己検査（戻したら終了コード 1／ファイルごと消えたら終了コード 1）も入れた。

      **残っているのは検算だけ。** 別の作業と同時に走っていて、いま
      `test_core` の `presentation`（`せんしの名前`）と `check_ui` の
      `auto_items`（12px の漢字）が赤い。どちらも `data/jobs.json` と
      `data/abilities.json` の**呼び名をかなから漢字へ変えた側**の影響で、
      S-6a とは無関係。**そちらが落ち着いてから通す。**

      **Gate:** 領域ごとに **文言が 1 文字も変わっていない**こと
      （移す前後で、実行して出る文字列の集合が完全一致することを検査で示す）。
      `check_terms.py` の監視範囲を `src/` 全体へ広げ、**わざと 1 件直書きを
      戻して終了コード 1** を確認する。`test_core` 全 suite ＋
      `test_world_events` ＋ `check_ui.py` が緑。`--play=120` で詰まり 0 件。

次の実装やプレイ監査で見つけた問題は、再現手順と `Gate:` を添えてここへ追加する。

---

済んだものは `[x]` を付けてから `docs/tasks_archive.md` へ移す。
行数の上限は置かない。未実装の項目を整理目的で消してはならない。
