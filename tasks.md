# やること

思いついた改善とリファクタリングはここに書き溜めて、上から順に処理する。
やり終えたら行を消し、必要なら「済んだこと」に理由だけ残す。

判断の根拠は次に置いてある。着手前に読む。

- 戦闘の設計 … `docs/battle_design.md`
- 潜る理由（前提） … `docs/premise.md`
- 作業の規約 … `AGENTS.md`

---

## 進み具合

**済んだこと 140 件 / 残り 7 件。**

終わった項目はここから消して `docs/tasks_archive.md` へ移すので、
このファイルに `[x]` は残らない。**動いているかどうかは下の「直近に済んだこと」で見る。**
理由まで読みたいときは控えのほうを開く。

### 直近に済んだこと

- 重なり検出は **1 フレームの中だけ**で見る。フレームをまたいで貯めると、
- **枠の中でも隣の列に食い込む。** てんしょく の ★ を 4 つ並べると隣の職業名に
- **作戦は戦闘をまたいで覚える。** 毎回 OFF から入れ直すのは、連戦のたびに
- **戦闘中の隅に長い説明を置かない。** 読まないのに場所を取るだけだった
- **オート中は間を詰める。** 自分で選んでいるときの間は読む時間として要るが、

## 次にやる（効き目の大きい順）

- [ ] **中断機能（ラン途中で保存して閉じる）。要望を受けたのに未実装のまま
      tasks.md から消していた（私の誤り）。** いまセーブは拠点の恒久データだけで、
      世界・位置・封・イベント・物語の進み・一時効果は保存していない。
      ワールド 1 周が 1 ランになった時点で、途中で閉じられないのは無理がある。
      **決定性があるので、種と進行度だけで復元できる可能性が高い**
      （`run_seed` から世界を作り直し、位置・封・イベント済み・拍・所持物を載せる）。
      丸ごと書き出すより安いはずなので、まずそちらを試して比べる。
      保存先は `user://suspend.json`（本体セーブとは分ける）。
- [ ] **デバッグ用の状態保存。これも未実装のまま消していた。**
      `--dev-save=名前` / `--dev-load=名前` で `user://dev/` へ。
      「この世界のこの場面」を読み直せると、不具合の再現とバランス確認が楽になる。
      中断機能と同じ書き出しを使い回す。
- [ ] **とうぞくの絵が忍者になっている。** 別の職として `ninja` があるので、
      とうぞくは軽装の盗人（頭巾ではなく目隠し布、短刀、袋）に描き分ける。
      `HERO_LOOKS`（`tools/gen_assets.py`）か取り込んだ候補絵のどちらかを直す。
- [ ] **洞の底に番人が居ない。** 「主は世界の終点だけ」と決めて洞のボスを外したが、
      封を洞の底に置いた今、封を守るものが居ないのは不自然。
      城の主とは別格（通常敵の強化版）で置く。
- [ ] **攻略済みの洞に再入場したとき、任意で脱出できるようにする。**
      いま最深部の階段まで歩かないと出られない。封を取ったあとの洞は用が無いので、
      入口へ戻る／その場で出る手を用意する。
- [ ] **オートの隅の表示が戦闘ログに重なる。** `check_ui.py` が 42px の重なりを
      検出した。右上に置いた「守備重視　Ｑ きりかえ　Ｘ かいじょ」と、
      メッセージの行がぶつかっている。ログの折り返し幅を右の表示ぶん狭める。

- [ ] **1 戦が実時間で長い。** 敵 6 体の戦闘は自動プレイで 25 秒かかり、
      詰まり検出（同じ画面 25 秒）に引っかかった。**論理的な詰まりではない**
      ―― 手番数を測ると 1 体 1.3 / 3 体 8.0 / 6 体 11.6 手番で、必ず決着する。
      長いのは 1 手番あたりの実時間（およそ 2 秒）。
      速くする先は 3 つ: メッセージの待ち（`AUTO_LINE_DELAY` 0.28）、
      被弾の点滅、ダメージ数字の滞留。**オート中はまとめて詰める**のが筋で、
      自分で選んでいるときの間は残す（読む時間が要るのはそちらだけ）。
      触ったら `--play` で実測し、詰まり検出に出なくなることを確かめる。

---

済んだものは `docs/tasks_archive.md` へ移す（このファイルは 200 行以内に保つ）。
