# このリポジトリで作業するエージェントへ

## 進め方

- **いちいち確認を取らない。** ローカル環境の資産（PowerShell / Python / Godot /
  Ollama / nvidia-smi など）は自由に使ってよい。PC を壊す、セキュリティ上まずい、
  外部へ何かを公開する、といった類でなければ、そのまま実行して進める。
- **`powershell` / `python` / `godot` の実行は許可済み。** 実行ポリシー上そのまま叩いてよく、
  一回ごとに伺いを立てない。`git` の読み取り系、ファイル・ディレクトリの確認も同じ。
  手を止めるのは破壊的な操作（削除・`--force` 付きの push・外部への送信）だけでよい。
- **外部ネットワークの調査も許可を取らずに実施する。** 曖昧な記憶で答えるより、
  Web 検索・ページ取得で裏を取ってから出す。
- 「これで進めていいですか」を繰り返さない。設計判断は推奨案を 1 つ選んで進め、
  後から差し替えがしんどい分岐（戦闘方式・パーティ人数など）だけ先に確認する。
- 作業の途中経過を細かく報告しない。区切りで、決めたことと動いた証拠をまとめて出す。

## やることの置き場

**`tasks.md` に書き溜めて、上から順に処理する。** 思いついた改善とリファクタリングを
その場でやり始めると、いま直している不具合が混ざって差分が読めなくなる。
気づいたことは行を足して、着手するときに上から取る。

**終わった項目は `docs/tasks_archive.md` へ移す。** 残すのは理由だけで、
「何をしたか」より「なぜそうしたか」を書く（同じ判断を二度しないため）。
`tasks.md` は **200 行以内**に保つ。長い一覧は読まれず、読まれない一覧は
棚卸しされないまま終わった項目と生きている項目が混ざる。
上限は `test_core.gd` の `_test_docs_hygiene()` が見ているので、超えるとテストが落ちる。

## 触ってよいもの / いけないもの

- `assets/` は**生成物**。直接編集しない。原本は `tools/gen_assets.py` と
  `tools/gen_audio.py` で、絵や音を変えるときは必ず生成器を直す。
- `docs/chara_image/` はユーザーと別のエージェントが置く素材置き場。**勝手に描き換えない。**
  ここに候補が置かれたら、`tools/gen_assets.py` が取り込んで `assets/` を作る。

  | 置くもの | 名前 | 条件 |
  |---|---|---|
  | 味方の歩行シート | `candidate_hero_<職業>.png` | 72x128（24x32 x 3 列 x 4 行） |
  | 敵 | `candidate_enemy_<敵>.png` または `_refined` | 64x64 以内 |
  | マップチップ | `candidate_tiles_<場所>.png` | 144x16（16x16 x 9 枚） |

  共通の条件は **15 色以内 / アルファは 0 か 255**。BGR555 に乗っていない色は
  取り込み時に丸める（実機に無い色は間違いではなく、まだ丸めていないだけ）。
  寸法・アルファ・色数のように直せない違反は例外で止める。
  マップチップは追加で「床と壁の色が RGB 距離 40 以上離れていること」を見る
  （綺麗でも、どこを歩けるか読めない地形は採用しない）。
  素材が無ければ ASCII マップから起こす方に落ちるので、生成は常に通る。
- `.godot/` は Godot の生成物。追跡しない。

## 崩してはいけない不変条件

1. **決定性**。同じシードからは地形・敵編成・ダメージ・行動順のすべてが一致すること。
   乱数は `DetRng` のみ。`randi()` や `Time` をロジックに持ち込まない。
   同時刻の行動順は id 昇順で確定させる。
2. **LLM をゲームロジックに入れない**。接続点は `src/game/chronicle.gd` の 1 か所だけ。
   生成が失敗しても遅くても、ゲームは完全に成立すること。
3. **失うもの / 残るものの境界**は `game_state.gd` の `end_run()` に集約する。
   ここを分散させない。
4. **SFC の規約**（BGR555 量子化 / 15 色サブパレット / 512x320 / 24x32 キャラ /
   ローパス + エコー）は `tools/sfc_art.py` と `tools/sfc_audio.py` が強制する。
   迂回しない。
5. **画面に出す言葉は `src/ui/terms.gd` に置く**。呼び名は何度でも差し替わるので、
   View に直書きしない（「残響」を「資源」に変えたときに全画面 grep する羽目になる）。
6. **文字は左上基準で置き、漢字は 14px 以上**。詳細は下の「文字」を参照。

## よく使うコマンド

```bash
python tools/gen_assets.py                            # ドット絵を生成
python tools/gen_audio.py                             # 音を生成
python tools/verify_audio.py                          # 音の検算（無音・音程・クリップ）
godot --headless --import                             # class_name を登録（新クラス追加後は必須）
godot --headless --script res://tests/test_core.gd    # 決定性テスト（常に緑に保つ）
godot --headless --script res://tests/balance.gd      # 到達率と勝率を実測する
godot --path . -- --shot=title                        # 画面を 1 枚撮って終了
godot --path .                                        # 起動

godot --headless --export-release "Windows Desktop" build/windows/RePrise.exe   # 配布物を書き出す
```

書き出しの前提（テンプレートの入れ方を含む）は README の「書き出す（ビルド）」にある。

`--shot=` に渡せる画面（`src/scenes/main.gd` の `_capture`）:

| 値 | 撮れるもの |
|---|---|
| `title` | タイトル |
| `stronghold` | 拠点 |
| `job` | 拠点の職業えらび（上級職の解放表示を含む） |
| `upgrade` | 拠点のアップグレード（資源 42 を仮に入れる） |
| `explore` | 探索 |
| `shop` | 道中の出店（出店のある階まで自動で降りる） |
| `battle` | 通常戦闘 |
| `commands` | 戦闘の とくぎ サブウィンドウ（技を全部与えて確認する） |
| `items` | 戦闘中のどうぐ一覧 |
| `menu` | 探索中のメニュー（どうぐ / つよさ / そうび） |
| `equip` | そうびの付け替え |
| `deep` | 深層の敵 |
| `boss` | 主の間 |
| `result` / `win` | 戦記（全滅 / 生還） |

## 自分で遊んで確かめる（自動プレイ）

```bash
godot --path . -- --play=180                       # 180 秒ぶん自動で遊んでコマ撮りする
godot --path . -- --play=240 --dev-level=8         # レベル 8 から始めて深層を見る
```

`src/dev/autoplay.gd` が**入力だけ**を流し込む。内部を直接叩かないので、UI が
どこかで詰まっていればそこから進まなくなり、詰まりがそのまま検出できる。
終了時に「画面の遷移」「詰まり」を出し、コマは `docs/preview/play/` に落ちる（追跡しない）。

実際にこれで見つかったもの: 遊んだ時間の 54% が戦闘だった（遭遇率を下げた）、
戦記の行が窓から溢れていた（折り返しを入れた）、探索が酔歩では 1 階から出られない
（`DungeonMap.route()` で階段へ向かうようにした）。

デバッグ指定（`src/dev/dev_cheats.gd`）。ランの初期状態だけを変え、決定性は壊さない。

| 引数 | すること |
|---|---|
| `--dev-level=N` | 全員をレベル N にする |
| `--dev-floor=N` | 地下 N 階から始める |
| `--dev-gear` | 装備を一式持たせて着せる |
| `--dev-items` | 道具を一式持たせる |
| `--dev-echo=N` | 恒久通貨を N 持たせる |
| `--dev-master` | 全職業の技を覚えた状態にする |

## 検証のしかた

- コードを足したら `test_core.gd` を通す。決定性に関わる変更は必ずテストを追加する。
- **数値（経験値曲線・敵の能力・階層補正）を触ったら `balance.gd` で測り直す。**
  勘で調整しない。現状は自動操縦で「主に挑めた 33% / 倒した 14%」に合わせてある。
  自動操縦は道具も出店も使わないので、ここで出る数字は人が操作したときの下限。
- 見た目を変えたら `--shot=` で撮って**実際に画像を見る**。目視せずに「できた」と言わない。
- 音を変えたら `verify_audio.py` を通す。耳で確認できないので機械検算に頼る。

## 戦闘まわりを触るとき

**必ず `docs/battle_design.md` を読んでから触る。** 往年の JRPG を調べて
「何が戦闘を面白くしているのか」をまとめてある。属性・状態異常・範囲の減衰・
敵の予告・ぬすむ の期待値は、すべてそこの結論として入っている。

- **他社作品の固有名詞を使わない。** 初版で呪文名に DQ の名前をそのまま置いてしまった。
  体系（語幹 + 段階の接尾）を借りるのはよいが、語は独自に作る。
- 技を足すときは「役割が既存と違うか」を確認する。強い通常攻撃を増やしても選択肢は増えない。
- 範囲攻撃には必ず減衰を入れる（少ないほど 1 体あたりが増える）。
- 数値を触ったら `tests/balance.gd` で測り直す。現状は 主に挑めた 33% / 倒した 14%。

## 文字とキャラの規約（実測で決めたもの）

- **漢字は 13px を下回ると読めない。** MS ゴシックで銀・響・深・階が団子になる。
  サイズ違いを並べて撮って確かめた結果、規約を `PixelUI` に置いてある。
  漢字を出してよいのは `SIZE_TEXT`(14) 以上、`SIZE_SUB`(12) は かな・数字専用。
  これが 384x240 から 512x320 へ上げた理由でもある。
- **文字の座標は左上で渡す**（`PixelUI.draw_text`）。`draw_string` はベースライン基準で、
  そのまま座標を渡すと窓枠に文字がめり込む。窓の中に置くときは
  `PixelUI.content(rect)` を基準にすること。枠の実際の厚みはここが吸収する。
- **24x32 の顔は目が命。細い目は必ず不気味に見える。**
  目は 2px 幅 x 3 段（睫毛 / 白目＋虹彩 / 虹彩の影）、まわりに肌を残し、
  前髪と目のあいだに額を 1 段入れる。口は描かない。
- **職業の描き分けは差し色ではなくシルエットで作る。** 色だけ変えると同じ人に見える。
  `HERO_LOOKS`（`tools/gen_assets.py`）に被り物・髪の長さ・肩・裾の差分を置く。
  基本の姿は列の役割を固定してある（x3..x6 と x17..x20 が髪、x7..x16 が顔と胴）ので、
  ここを崩すと差分が全部ずれる。

## 落とし穴

- オートロードは `--headless --script` 実行時にグローバル登録されない。
  テストから触るものは静的クラスにする（`Database` がその例）。
- `class_name` を新規追加したら `--import` を挟まないとテストが「識別子が無い」で落ちる。
- PowerShell 5.1 では `&&` が使えない。`;` と `if ($?)` を使う。
  ネイティブ exe への `2>&1` も避ける（成功しても失敗扱いになる）。
- 撮影が `accesskit_windows` のパニックで落ちることがある（メモリ資源の一時不足）。
  `godot --path . --accessibility disabled -- --shot=...` で回避できる。
- ウィンドウタイトルは `DisplayServer.window_set_title()` で入れても戻る。ルート
  Window があとから自分の `title` を流し込むため。`get_window().title` へ入れること。
  デバッグ実行では末尾に ` (DEBUG)` がエンジン側で足される（これは消せない）。
- エクスポートテンプレートは**エンジンと完全に同じバージョン**でないと使われない。
  Godot を上げたら入れ直す。入っていないと `--export-release` は
  「テンプレートが見つかりません」で落ちる（プリセットの問題ではない）。
- 書き出す前に `gen_audio.py` を回す。WAV は追跡していないので、忘れると
  **音の無いビルド**ができあがる（エラーは出ない）。
- **`config/name` を変えると `user://` の保存先も変わる。** `%APPDATA%\Godot\
  app_userdata\<config/name>` なので、改名時は既存の `save.json` を手で移す。
- 撮影のうち `result` / `win` / `boss` は `end_run()` を通るのでセーブを書き換える。
  戦績を汚したくないときは事前に `save.json` を退避する。
- 鏡像で綴じるドット絵（`build_monster`）は、**右端が合わせ目**。体が中心をまたぐ行の
  右端に輪郭色を置くと、合わせ目で線が二重になって胴が左右に割れる。
- 48px の枠に骨格を描き込むと必ず潰れる。特徴を 2〜3 個に絞って
  シルエットで読ませる（骸骨兵を 3 回描き直して、頭蓋 1 個に絞って解決した）。
