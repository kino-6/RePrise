# 済んだことの控え

`tasks.md` から降りてきたもの。**理由だけを残す。**
同じ判断を二度しないためにあるので、「何をしたか」より「なぜそうしたか」を書く。
新しいものを上に足す。

---

## 2026-07-30
- 強い魔法（全体攻撃と威力 130 以上の魔法）で画面を白く飛ばすようにした。
- サブウィンドウを縦に開く演出を入れた。一瞬で出ると板が貼られたように見える。
- 自動プレイの最後に最深階・戦闘数・ランの終了数を出すようにした。
  走らせるたびに数字で比べられる。

- 敵 AI に弱点と状態異常の使い分けを入れ、**わざと最善を打たない**ようにした。
  素点の最大だけを選ばせたら勝率が 14% から 1% に落ちた。最善を打ち続ける相手は
  強いのではなく理不尽になる（DQ4 の AI がそう作られている理由）。上位 45% から引く。
- 宝箱から装備と道具が出るようにした。金だけでは開ける手間に対して薄い。
- 逃走の見込みをコマンドに出した（「にげる 92%」）。読めないと賭けにすらならない。
- ダメージの数字を相手の上に出し、属性ごと・状態異常ごとに音を分けた。
- 画面の切り替えに暗転を挟んだ。無いと画面が「差し替わった」ように見える。
- オート戦闘を `src/battle/auto_tactic.gd` へ切り出し、作戦を 2 種にした
  （いのちだいじに / ガンガンいこうぜ）。判断と表示を分けたので balance.gd と
  方針を揃えるときもここだけ見ればよい。
- 一覧のスクロールとカーソルを `MenuList`、通知を `Notice` に集約した。
  同じ数式が 4 か所にあると、1 か所だけ直して他が溢れる（職業を 15 に増やしたとき
  実際にそうなった）。
- セーブを辞書とファイル入出力に分け、version 1 からの移行を検査した。

- 呪文名を他社の固有名詞から独自の造語へ。体系は借りてよいが語は借りない。
- 遭遇率を下げた。自動プレイで遊んだ時間の 54% が戦闘だったため。
- 戦記の行を折り返した。覚えた技を全部並べるので長さが読めず、窓から溢れていた。
- `DungeonMap.route()` を足した。酔歩では 3 分遊んでも 1 階から出られなかった。
- 入力をイベント判定に統一した。`_unhandled_input` の中で
  `Input.is_action_just_pressed()` を見ると、1 フレームに 2 つ届いた入力を取りこぼす。
- タイルの取り込みに「床と壁の明度差」の判定を入れた。綺麗でも遊べない地形を
  黙って通さないため。判定に落ちたら従来の ASCII タイルへ落ちる。
- 窓を半透明にした（不透明度 0.72）。後期 SFC の画面が richer に見えた理由は
  PPU のカラーマスによる半透明で、不透明な板はファミコンの作法。下の絵が透けるだけで
  世代が変わる。0.7 を下回ると文字の影が背景に負ける。
- 戦闘の背景を縦グラデーションにし、被弾を点滅で見せるようにした。
  当たった相手は BattleSystem が id で持つ（ログの文字列を読むと文言を変えた瞬間に消える）。
- 職業を 15 に増やした（絵は 15 職ぶん取り込み済み）。熟練一覧と職業えらびは
  枠から溢れたのでスクロールと 4 列に組み替えた。
  データの取りこぼしは `_test_data_integrity()` が見る（存在しない技・循環する解放条件・
  立ち絵の欠け）。
- 味方のドット絵を外で描いたシートに差し替えた。ASCII マップでは後期 SFC の密度に
  届かなかった。取り込み口は生成器に置き、SFC の制約（72x128 / 15 色 / アルファ 2 値 /
  BGR555）を検算してから採用する。

- 敵を 22 体（＋主 4 種）にした。絵は Codex の候補を取り込み、能力値は
  `balance.gd` で挟み込んで決めた（3% → 15% → 43% → 19% → 36%）。
  攻撃力 1 点で結果が倍近く振れるので、1 回測って止めてはいけない。
- 主が 4 種から選ばれるようになった。地下 10 階の相手が毎ラン変わる。
- 控えと編成を入れた。名簿は 8 人まで、連れて行くのは 4 人。恒久通貨で仲間を迎える。
  「留守番した者は熟練が積まれない」ので、誰を連れるかが次のランに効く。
  出撃メンバーの自動補充は**空のときだけ**にした。減るたびに埋め直すと、
  外した者がすぐ戻ってきて編成にならない（テストが捕まえた）。
- 戦記をローカル AI（Ollama）に繋いだ。テンプレートを出したうえで差し替えるので、
  届かなくても画面は空白にならない。Qwen3 系は think:false が必要（思考ブロックが
  num_predict を使い切って本文が空で返る）。
- 遭遇時に画面を 2 回白く瞬かせるようにした。暗転だけでは緊張が出ない。
- 自動プレイが装備の付け替えまで踏むようにした。乱数の入力では 4 段のメニューを
  抜けないので、決まった手順を混ぜる。`--dev-stock`（手持ちに入れるが着せない）で
  道筋そのものを確かめられる。
- マップチップは差し替えを見送った。生成器側で床を暖色へ寄せる補正も試したが、
  地下で 39.0（条件は 40.0）にしか届かず、これ以上動かすと絵が壊れる。
  描く側で離すのが正しい。
- 新しい職に「その職らしい技」を 1 つずつ与えた（つらぬき / ばくやく / ときとまれ /
  じゅうのほうこう / けんじゃのいちげき / きまぐれ）。既存技の組み替えだけだと、
  就いても手触りが変わらない。守り無視（pierce）と攻めながら癒す（mend）は
  戦闘側に実装が要った。
- 階層ごとに地形を変えた（地下 7 階から雪原）。同じ絵で 10 階ぶん潜ると
  進んでいる感じが出ない。素材が無い場所は既定に落ちるので、通った絵だけが入る。
- 帝国系の敵 12 種と主 3 種を取り込んだ。足した直後は 46% に緩んだ（1 体あたりが
  既存より軽く、深い階の圧が下がった）ので、中〜深を一段上げて 31% に戻した。
- 候補の可否を測る `tools/check_candidates.py` を置いた。別のエージェントが素材を
  置いたらこれを回す。落ちた理由が数値で出るので、そのまま描き直しの指示になる。
- tasks.md の先頭に進み具合を出すようにした（`tools/tasks_status.py`）。
  終わった項目を控えへ移す運用だと、tasks.md だけ見ると「ずっと [ ] のまま」に見えて
  進んでいないように読める。
- 探索メニューを半透明にして、下のダンジョンを見せるようにした。
  塗りつぶすと場面が切り替わったように見えるが、透けていれば
  「歩みを止めて鞄を開けている」ように見える。
- 主の間の扉が隣に来たら知らせるようにした。扉は地形に紛れるので、
  気づかないまま踏んで開幕から不利、という事故が起きていた。

## 2026-07-30（遊んでもらって出た指摘）

- **遭遇したあとも歩けて、そのまま階段へ降りられた。** 暗転を入れたときの取りこぼし。
  切り替えが即座だったころは起きなかったが、幕の裏で切り替える形にすると
  その 0.3 秒のあいだ入力が生きたままになる。幕を下ろす前に歩みを止める。
- **メニューを「とじる」で閉じられない。** 実際は閉じていて、同じキーを探索側が
  拾って開き直していた。UI をまたぐ入力には一拍おく（探索へ戻った直後 0.18 秒）。
- **つよさ の Lv がキャラのものか職業のものか読めない。** 人のレベル（ランで失う）と
  職業の熟練（持ち帰る）は別物なので、必ず並べて書く。
- **出店の品がほとんど買えない。** 稼ぎを測っていなかったのが原因。balance.gd に
  「その階までの累計ゴールド」を足したところ、3 階までで 141 に対して店には
  120〜170 の品が並んでいた。値段と出現階を稼ぎに合わせ直した。
- **宝箱と盗むから金しか出ない。** 確率が低すぎた（宝箱で物が出るのは 45%、
  盗みの成功は 45%）。3 回開けて 3 回とも金なら、中身の種類は無いのと同じ。
  宝箱 68% / 盗み 60% へ上げた。
- **オートの止め方が画面に無い。** 始め方だけ見えて止め方が見えないのは不親切。
  作戦名と「Ｘで かいじょ」を出し、キャンセルは送りより解除を優先する。
- **オートが通常攻撃を連打しているだけに見える。** 実際「効率が最大の技」しか
  見ていなかった。いのちだいじに（起こす→回復→状態を治す→MP を使わず削る）と
  ガンガン（弱点→範囲→効率）に作り直し、**基準を画面に出した**。
  何をしているか読めないオートは、賢くしても信用されない。
- **裸でダンジョンに潜っていた。** 職業ごとの初期装備を毎ラン支給する
  （持ち帰る資産ではないので、恒久強化は能力値に触れないという前提は崩れない）。
  これで 31% → 66% に緩んだので、階層補正を 9%/階 の一次から
  11%/階 + 深さの二次（/14）へ変えて 31% に戻した。

## 2026-07-30（BGM と、潜る理由）

- **BGM のループが短くて安く聞こえる。** 戦闘 8 小節（約 12 秒）は、1 戦のあいだに
  3 回も同じところへ戻ってくる長さだった。A→B→A' に組み直し、
  戦闘 24 小節（37.6 秒）/ 道中 32 小節（60.7 秒）/ 拠点 32 小節（76.4 秒）へ伸ばした。
  **長さだけでは足りない。** SFC 期の曲が短いループでも保っていたのは中に抜き差しが
  あったからで、B 部で打楽器のハイハットを抜き、和声を長調側へ振り、
  対旋律（笛）と内声の分散和音（鐘）を足し、小節の切れ目に埋めを入れた。
  `tools/verify_audio.py` は 3 曲とも通る（無音・クリップ・直流・起伏・和音）。
- **ダンジョンに潜る理由が無かった。** 仕組み（レベルと装備を失い熟練は残る、
  資源は全滅しても入る、主は毎回別）だけが先に在って、言葉が無い状態だった。
  設定はユーザーの領分なので**案として** `docs/premise.md` に置き、
  「すでに動いている仕組みを説明できること」だけを条件にした。
  奈落は潜るたび作り直されるので前回の続きが無い／地下で得た力と得物は返るが
  体が覚えた癖は返らない／砦は奈落から吹き上がるもので建っている、の 3 点。
  新しい固有名詞は増やしていない（「奈落」は既に奈落の番人が使っている）。
  文言は `src/game/lore.gd` に集約したので、気に入らなければそこだけ差し替わる。
  画面に出すのは 3 か所（出撃の 3 行 / 資源の 1 行 / 戦記 AI の前提）に絞った。
  **戦記 AI には事実（facts）と分けて渡す。** 混ぜるとモデルが設定のほうを膨らませる。
- 棚卸で気付いたが、毒の持ち越し・MP 回復手段・設定画面・キー割り当て・セーブの控えは
  実装済みなのに tasks.md へ残ったままだった。**済んだ行を消すのは実装と同じ作業。**
  残っていると、残り 8 件に見えて優先順位の判断を誤る。
- **起動のたびに「セーブが読めなかったので控えから復帰した」が出ていた。**
  `user://save.json` が消え、控えだけが残っていた。控えは仕事をしていたので進行は
  無事だったが、**復帰しても本体を書き戻していなかった**ので警告が鳴り続ける。
  常時鳴っている警告は本物の異常を隠すので、これは直す価値がある。
  原因側は `FileAccess.open(WRITE)` が**開いた瞬間に中身を捨てる**こと。本体を直接
  開いて書くと、書き終わるまでセーブが存在しない時間ができる。そこで落ちれば消える。
  控えへ寄せる → `.tmp` へ書く → 読み返して確かめる → 差し替える、の順に変えた。
  どの時点で落ちても本体か控えのどちらかは必ず生きている。
  併せて、控えから復帰したらその場で本体を作り直すようにした。
  テストを足すために読み書き先を `use_save_paths()` で差し替えられるようにしてある。
  **定数のままだとセーブのテストが実データを潰す**（過去に名簿を 1 人に減らした一件）。
  検査 154 → 165 項目。

## 2026-07-30（ワールドごとローグライクへ）

- **コンセプトを読み違えていた。** 作っていたのは「地下 10 階の潜行」で、
  ダンジョンローグライクだった。狙いは**世界ごと作り直すローグライク**で、
  不思議のダンジョン系ではなく、歩けるワールドマップを持つ SFC 期の JRPG の形。
  設計は `docs/world_design.md`、前提は `docs/premise.md`（奈落案は破棄）。
- **`floor_number` は「難度のスカラー」でしかなかった。** 100 か所に出てくるが、
  中身は敵プールの門・能力倍率・戦利品の等級・出店の品揃え・表示だけ。
  **「危険度 1..10」に読み替えるだけで、data/*.json の floor_min も
  実測で合わせた難度曲線もそのまま生き残った。** 危険度は門からの陸路距離で決まる。
- **ExploreView を分けなかったのが効いた。** あそこは歩き・カメラ・遭遇・描画を
  持っているが地図の中身に依存していない。共通の `FieldMap` を切り出して
  `DungeonMap` / `WorldMap` の親にしただけで、**同じ 1 本で世界も洞も歩ける**。
  ワールド用にもう 1 本作れば、片方だけ直したバグが必ず出ていた。
- **生成物は必ず到達可能にする。** 山で城が孤立する世界が出るので、生成の最後に
  門から城まで歩けるかを測り、閉じていたら山を削って繋ぐ。
  詰む世界を 1 つ出すだけでそのランが丸ごと無駄になるので、テストで守る（173 項目）。
- **地形に通行以外の意味を持たせる。** 草原 1 / 丘 2 / 森 3 の遭遇重み。
  全部同じにすると地形がただの模様になる。
- **タイルは 15 色で全部を賄う。** SFC の 1 サブパレットの制約。22 色書いて弾かれた。
  割り振りに意図を持たせ、**通れない地形（海・山）は寒色、通れる地形は暖色寄り**に
  した。ダンジョンタイルが読めなかったのは明度だけ変えて色相を揃えたせいだった。
  自動で測って出す（いま最も近いのは森と山で 66.5、基準 40）。
- **文字列を当てにした集計は表示を変えると黙って壊れる。** 自動プレイの「最深」は
  状態の 1 行から「地下」を探していたので、城まで行ったのに 1 のままだった。
- **`tests/balance.gd` は通るのに古い模型を測っていた。** 壊れているより悪い。
  tasks.md の先頭に置いた。

## 2026-07-30（balance.gd をシミュレータにする）

- **「測る道具を直す」のではなく「シミュレータとして使う」に変えた。**
  旧 balance.gd は `1 階につき 4 戦` という**書き写した模型**を持っていて、
  通るし数字も出るのに存在しない構造を測っていた。書き写しがある限り、
  直しても必ずまたずれる。そこで**遊ぶときと同じ部品しか呼ばない**形にした
  （世界生成・経路探索・地形の重み・遭遇判定・敵編成・戦闘・オートの判断）。
  **規約は「シミュレータに数値を書き写さない」。**
- **その前提として、遭遇の判定を `Encounter.should_meet()` へ移した。**
  `ExploreView` の中にあると、測る側が同じ式を書き写すことになる。
  同じ式を 2 か所に置いた時点で、片方を直したときに黙ってずれる。
- **出すのは 1 つの数字ではなく分布。** 「主に挑めた 31%」だけでは、
  遠くて届かないのか、届いても勝てないのかが分からない。倒れた危険度の
  ヒストグラムと「倒れた時の Lv と危険度」を並べると、原因がその場で読める。
- **回した瞬間に世界が成立していないことが分かった。** 城に着けたのは 1%、勝ちは 0%。
  倒れるのは危険度 5 前後で、そのとき Lv 3.3。原因は構造的だった ——
  **危険度は「距離」で上がるのに、強さは「戦闘数」で上がる。**
  75 歩の横断で戦闘は約 7 回、危険度は 8 歩ごとに +1 なので、
  危険度 +1 あたり 0.8 戦しかない。地下 10 階のころは 1 階 4 戦 = 危険度 +1 あたり
  4 戦だったので、**5 倍足りていなかった**。全周させても死ぬ場所は変わらなかった
  （途中で削られて死ぬので、周回では解決しない）。
- **経験値とゴールドが危険度で伸びていなかったのが本体。** 敵の能力値だけ
  `100 + (n-1)*11 + (n-1)²/14` で伸ばし、報酬は素の値のままだった。
  階が深まるほど高い経験値の種族に入れ替わることで自然に追いついていたので、
  ダンジョンでは露呈しなかった。**能力値と同じ倍率を報酬にも掛ける**形にした
  （2.2 倍強い敵は 2.2 倍の経験をくれる）。倍率の式は 1 か所に集約。
- **伸び幅は挟み込みで決めた**（`EXP_GAIN`）。140 は全周で 8% しか勝てず辛い、
  260 は全周 70% で自動操縦が素通り、**180 で 直行 0% / 寄り道 6% / 全周 22%**。
  歴史的に手応えが良かった帯（自動操縦 19%）とほぼ同じところに落ちた。
- **ゴールドは経験値と別の数字にした。** 同じ倍率だと城に着く頃に 4500 持っていた
  （品物は 36〜186）。**役目が違う。** 経験値は土地に見合う強さを追いつかせる
  ためのもので、伸ばさないと世界が成立しない。ゴールドは買い物の判断を作る
  ためのもので、伸ばしすぎると全部買えて選ぶ意味が消える。
- **副産物として難度曲線が面白くなった。** 直行は 21% 着くが 0% 勝てない、
  寄り道で 6%、全周で 22%。**「急げば着くが勝てない」が数字として在る**ので、
  寄るか急ぐかが本物の判断になった。シミュレータは直行で勝てるようになったら
  警告を出す（判断が死んだ合図なので）。
- 所見の判定は**いちばん強い方針**で見る。直行を基準にすると「主が硬い」と
  毎回誤診する（直行が勝てないのは設計どおり）。

## 2026-07-30（生物相・てんしょく・マップチップ）

- **世界が適当に見えたのは、1 マスずつ危険度から地形を抽いていたから。**
  草原と森と丘が均一に混ざった斑模様になり、どこも同じ顔になる。地図が世界に
  見えるのは**同じ地形がまとまって続く**からで、「ここは森林地帯」と読めることが
  情報量になる。種を撒いてボロノイで面に染めた。
- **生物相は「その地帯の平均の危険度」で決める。** 種の危険度で決めたら、
  危険度 4 で引いた湿地が門（危険度 1）まで広がった。面を先に確定させてから
  中身を選べば、地帯と危険度が必ず噛み合う。
- **生物相に意味を持たせる。** 敵の抽選を絞る（雪原は氷に強いもの、火山は火に
  強いもの）。**指定の無い敵はどこにでも出る**ので、絞ってもプールは空にならない。
  空にすると、その土地で一度も戦えないまま通り抜けられてしまう。
- **私が承認されていない概念を設計根拠にしていた。** 「代償」「失うことが主題」は
  ユーザーの言葉ではなく私の創作で、それを根拠にラン中の転職にレベルリセットを
  付けた。指摘を受けて撤回。**もっともらしく見える設計原則を勝手に足さない。**
  あわせてユーザーの明示を AGENTS.md の不変条件に入れた:
  - **永久資産（熟練・技・資源・名簿）に手を付けない。** ラン内資源は失ってよい。
  - **利便性を損なう代償は、ラン中であっても入れない。**
  てんしょくはどこでも自由・取り上げるものは無し、に落ち着いた。
- **マップチップは却下せずに直す形へ変えた。** 床と壁の RGB 距離が 40 未満の
  candidate を 3 つ見送っていたが、**この判定は直せる違反**だった。取り込みは
  元々「丸められるものは丸めて通し、寸法・アルファ・色数のように直せないものだけ
  止める」作りなので、距離を止める側に置いていたのが誤り。
- **引き離すときは床と壁の両方を動かす。** 壁だけ暗くしたら、草原が夜の洞窟に
  なった（元の絵が既に暗かったため）。床を明るく・壁を暗く同じ段だけ開けば、
  中間の明るさが保たれて元の調子が残る。色相は触らない（絵が壊れる）。
  結果、5 種すべて取り込めた（草原 9.0 → 49.4、湿地 3.4 → 45.4、火山 7.2 → 47.4）。

## 2026-07-30（戦闘 UI を後期 SFC へ）

- **半透明にしただけでは後期 SFC にならない。** 窓の不透明度は前から 0.72 に
  してあったのに、画面はファミコンのままだった。DQ6 の窓が richer に見えたのは
  透過**に加えて**「中の階調」「面取り」「落ち影」が全部乗っていたから。
  単色の板に枠を描くと、透けていてもファミコンに見える。
- **階調はわざと段にする。** SFC の階調は実際に段になっていて、滑らかに繋ぐと
  むしろ時代がずれる（12 段）。
- **面取りは 1px の明暗 2 本で足りる。** 内側の上辺・左辺に明線、下辺・右辺に
  暗線。枠の絵（window.png）を描き替えずに厚みが出る。
- **`draw_window` に入れたので全画面が一度に richer になった。** 拠点も店も
  メニューも同じ関数を通っている。UI の質は 1 か所に集めておくほど上げやすい。
- **戦闘背景は一枚の階調では「場所」にならない。** 空と地面を別の階調にして、
  境目に地平線を 1 本入れる。これだけで敵が「立っている面」ができる。
  地面の色は生物相ごとに変えた（雪原で戦っているのに土色だと、生物相を
  持たせた意味が薄れる）。
- **足元の影が無いと敵は宙に浮いて見える。** 楕円ひとつで直る。

## 2026-07-30（町と NPC、ゲージの階調）

- **ゲージは 5px でも管に見せられる。** 上 1px を明るく、下 1px を暗くするだけ。
  減ったぶんを暗く残す残像も足した（数字を読まずに減少量が幅で分かる）。
  行動順の札は 10 枚並ぶので、平らだと画面の上端がそのままファミコンに見えていた。
- **町を品書きだけにしていたのは、町を場所にしていなかったということ。**
  中を歩けるようにすると、宿・店・人が別々の場所になり、「誰に話すか」
  「何を先にするか」がそのまま行動になる。地図の型が `FieldMap` で揃っていたので
  `ExploreView` がそのまま歩けた（型を揃えておいた分がここで返ってきた）。
- **町は迷わせない。** 洞と違って町は目的地であって迷路ではない。外周を壁で囲った
  1 枚の広場にして、宿・店・出口を広場から見える位置に固定した。
  迷う町は、用があって寄った人間にとってただの足止め。テストで「宿・店・出口に
  必ず辿り着ける」を守る。
- **町の人は通れないマスに置く。** 押し当てて話す（洞の宝箱と同じ作法）。
  すり抜けられると話しかけられない。これもテストで守る。
- **同じ人物像を並べない。** 全員が世間話をすると町が背景になる。宿の主・店番・
  物知り・子ども、と役を散らした。物知りの一言は生物相の手掛かりになっていて、
  `docs/quest_design.md` の「町で聞ける」への足場になる。
  絵は職業のスプライトを役に割り当てて借りた（新しい絵を起こさない）。
- **町を出たら 1 マス外へ降ろす。** 中に居た場所（町のマス）へ戻すと一歩動いて
  また入る。人なら「出たのにまた入った」で済むが、自動プレイは出入りを繰り返して
  先へ進まなくなった（100 秒で戦闘 1 回 → 直して 12 回）。DQ でも町を出れば外に立つ。
- **宿は無料。** 「利便性を損なう代償を入れない」に従う。町まで戻ってきた手間が
  そのまま代金、という扱い。

## 2026-07-30（封 3 つと、生成器の自己検算）

- **生成器に自分の出力を疑わせる。** 世界は毎回違うので、目で見て確かめられるのは
  ごく一部でしかない。`WorldGenerator.verify()` が到達性と構造を測り、
  落ちたら種を振り直して作り直す（最大 12 回）。テストは 59 個の世界を通す。
  見ているのは 城まで歩ける / 封が 3 つ / 封の洞すべてに歩ける / 帯が 3 つに
  分かれている / 歩いて行ける町がある、の 5 点。
- **検算そのものもテストする。** 通す側だけ試すと、いつも空を返す検算でも緑になる。
  封を消した世界・帯を偏らせた世界を食わせて、ちゃんと落ちることを確かめる。
- **封は数（3）と帯（浅・中・深）だけ固定。** ここが固定だから 1 ラン の長さと
  難度曲線が読める。名と由来は語彙表 × DetRng で毎回変わる（AI 無しで成立させた。
  AI が来たらこれは捨てるのではなく、落ちたときの受け皿として残す）。
- **城は封が残っていると開かない。** ここを通すと洞へ寄る理由が宝箱だけに戻る。
  シミュレータでも同じ門を通す（測る側と遊ぶ側で同じ構造）。
  結果は 直行 0% → 封だけ狙い 6% → 全周 14%。**急げば城には着くが扉が開かない**が
  数字として在る。直行で勝てるようになったら警告を出す。
- **物知りだけが封の在り処を話す。** 全員が案内役だと町が掲示板になる。
  ExploreView は世界の事情を知らないので、文章は main.gd から差し込む。
- **設定画面から出られなくなっていた。** 自動プレイが 25 秒張り付いた。原因は 2 つ。
  (1) 割り当て待ちが**全ての入力を飲み込む**ので、始めたら取り消せない。
  (2) 自動プレイのモード分岐に SETTINGS が無く、confirm を押し続けていた。
  キャンセルで割り当てを取り消せるようにし（アクションでも効くように。
  自動プレイは InputEventAction を流すのでキーイベントだけ見ていると出られない）、
  他の操作に割り当て済みのキーは断るようにした（重ねると自分を閉じ込める）。
- **町は閉じた広場なので酔歩では出口に当たらない**（40 秒居座った）。
  自動プレイは町に居るあいだ出口へ向かわせる。戦闘 2 回 → 11 回に戻った。

## 2026-07-30（検算層と、AI の窓口を 1 本に）

- **AI を繋ぐ前に検算層を作る。** 繋いでから作ると、変な出力を見るたびに
  プロンプトをいじる方へ流れる。プロンプトは頼みごとであって保証ではないので、
  最後は受け取る側が弾くしかない。`src/game/quest_text.gd`。
- **テストは「落ちるべきものが落ちること」を主に見る。** 通す側だけ試すと、
  何でも通す検算でも緑になる。長すぎる名・数字の混入・英字・記号・
  他社の固有名詞・重複を食わせて、全部落ちることを確かめた。
- **項目ごとに落とす。** 1 つ変でも全部捨てない。封の名が 1 つ駄目でも残り 2 つは
  使える。落ちた項目だけテンプレートに残す。
- **AI は構造に触らない**ことをテストで固定した。名と一文を当てたあとも
  位置・帯は動かず、`WorldGenerator.verify()` を通ったままであること。
- **HTTP の接続点を 1 つにした。** `LocalAI` だけが Ollama を叩き、戦記も
  クエスト文もそこを通る。2 つ持っていたときは、片方だけ think を切り忘れる／
  片方だけタイムアウトを直す、という壊れ方をする形になっていた。
- **実機で通した。** Ollama から JSON が返り、検算が 6 項目すべて採用、
  ゲームはそのまま動き続けた。戦記も同じ窓口で動く。
- **待たせない。** 世界はテンプレートの名前で完成済みで、数秒後に届いたら
  表示だけ差し替える。構造を先に確定させてあるからこれが成立する。

## 2026-07-30（Codex のイベントカタログを本体へ繋ぐ）

- **効果は JSON に数値を書かず、トークン → 実数の変換を 1 か所に置く。**
  Codex の分離をそのまま守った（`src/quest/event_effects.gd`）。
  バランス調整と世界生成が分かれているので、数を触るときに JSON を開かない。
- **知らないトークンを黙って無視しない。** これが今回いちばん大事な決めごと。
  素通りさせると選択肢が「押しても何も起きない」になり、しかも画面上は成功に
  見える。全 775 件のトークンが解決表にあることをテストで突き合わせ、
  まだ効果を作っていない 11 個は `INFORMATIONAL` に**明示的に**置いた。
  無視しているのではなく「まだ作っていない」と分かる場所に在ることが要点。
- **払うものを選ぶ前に見せる。** 選んでから「そんなに減るのか」と気づく作りだと、
  選択が賭けではなく罰になる。costs / risks / rewards を行と詳細窓の両方に出す。
  払えない手は選ばせず、理由（たりないもの）をその場に出す。
- **必ず立ち去れる。** キャンセルで見送れて、踏み直せばまた開く。
  一方通行の選択を道端に置くと、事故で押しただけでランが壊れる。
- **一覧の 1 行は枠に収まる長さで切る**（`PixelUI.clip`）。全部並べたら溢れた。
  溢れた文字は読めないので、出さないほうがよい。中身は詳細窓に回す。
- **`verify()` に「置いたイベントに歩いて行けるか」を足した。** 届かないイベントは
  置いた手間がそのまま無駄になる。24 個の世界で 72 件、全部到達可能。
- **イベントが残す効果はすべてラン内資源に置いた**（遭遇率・品数・一時的な力）。
  永久資産に触れないという決めごとを跨がないため。
- 撮影の近道が `GameState.world_pos` を当てにしていて空振りした。
  **場所は渡された引数を見る。** 立っている場所に頼ると、まだそこへ立っていない
  呼び出し（撮影・テスト）で黙って何も起きない。

## 2026-07-30（UI の重なりとはみ出し、オートのキー）

- **値をラベルから固定の位置に置くと必ず重なる。** つよさ の「しょくぎょうの 熟練」が
  値と衝突していた。**列の右端で揃える**形に変えれば、ラベルの長さが変わっても
  ぶつからない。長い 2 行は 2 列の格子に入れず、幅いっぱいの独立した行にした。
- **右端で揃えるときは「窓の右端」ではなく「隣の絵の左」まで。** 銀の砦の
  「じゅくれんど」を窓の右端へ寄せたら、立ち絵の下に潜った。
- **選択カーソルは 2 倍で描く。** 8x8 の等倍は 14px の文字の隣で小さすぎて、
  どの行を選んでいるか一目で分からない。整数倍なのでドットは崩れない。
  ただし寄せ直すのは**下へだけ**。左へ引くと窓の縁を跨ぐ。
- **オートは A キーでどの階層からでも切り替える。** コマンド欄の末尾まで下がらないと
  触れないと、連戦のたびに 6 回下げることになる。止め方も同じキーで対称にした。
  入口が 2 つでも中身は `_cycle_auto()` 1 つに寄せる。
- 「あそびびと」は誤り。**あそびにん**。

## 2026-07-30（はみ出しの検出ゲート、オートのキー、NPC の絵）

- **「枠から文字がはみ出る」を機械に見つけさせた。** 何度も指摘され、直しても
  別の画面で再発する類なので、目視をやめて測る側を作った。`PixelUI` が
  `draw_window` の内側を覚え、`draw_text` のたびに「始点を含むいちばん小さい窓」に
  収まっているかを見る。**呼び出し側は 1 行も変えていない。**
  `python tools/check_ui.py` が全画面を回して、1 件でもあれば終了コード 1。
  入れた直後に、指摘どおり てんしょく の ★ が 12px はみ出しているのを検出した。
- **オートが切り替えられなかった原因は 2 つ。** A に割り当てたが A は WASD の
  「左」でもあり取り合いになっていた（→ Q へ）。さらに状態を COMMAND / LIST /
  TARGET に限っていたが、**オートが動き出すとその状態を外れる**ので、一度入れたら
  二度と切り替えられなかった。**止め方が無いオートは信用されない**ので、
  切り替えは戦闘中いつでも通す。
- **NPC に主人公の絵を借りていた。** 町の中に自分と同じ姿が 4 人立っていた。
  `docs/chara_image/candidate_npc_*.png` が用意されていたのに使っていなかった。
  専用の絵（宿の主・店番・物知り・斥候・衛兵）を取り込んだ。
  **借りて済ませた時点で「町に人が居る」にはなっていなかった。**

- **`encounter.gd` が文字化けした符号で保存されていた。** 同名の敵に付ける
  接尾辞（Ａ/Ｂ/Ｃ）がそのまま画面に出て崩れていた。**コメントだけでなく
  利用者に見える文字列が壊れていたのが問題。** 他の 40 ファイルは無事だったので、
  この 1 ファイルだけの事故。日本語を含むファイルは UTF-8 で保存する（BOM 無し）。
- **オートの説明が記号の羅列だった**（「起こす→回復→状態を治す→…」）。
  優先順位を矢印で並べても読み物にならない。文にする。
  **画面に出す説明は、仕様の要約ではなく人が読む文であること。**

## 2026-07-30（敵の上限 3 → 6）

- **「なぜ 3 なのか」に答えが無かった。** git で元のファイルを確認したが、
  `MAX_ENEMIES := 3` には注釈が一切なく、初期に置いてそのまま見直していない
  数字だった。SFC 期の JRPG は 6〜8 体を並べていたので 3 は狭すぎ、
  範囲技と単体技の選び分けが「2 体か 3 体か」でしか起きていなかった。
- **3 に縛られていたものは 3 つあったが、どれも結果であって理由ではない。**
  接尾辞が Ａ/Ｂ/Ｃ の 3 つだけ（4 体目が無名）、範囲技の減衰表が 1〜3 だけ
  （4 体以上が減衰なしで得）、戦闘画面が 1 列等間隔（5 体以上で絵が重なる）。
  順に、接尾辞を 6 つへ、減衰を式へ、5 体以上は 2 段へ。
- **数だけ増やして強さを据え置くと成立しない。** 6 へ上げた直後、シミュレータで
  全方針 0% になった。**群れで出るものは 1 体ずつ弱い**、というのが往年の作法で、
  ここを式にすると「1 体の強敵」と「6 体の群れ」が別の脅威になり、
  範囲技と単体技の選び分けが初めて意味を持つ。経験とゴールドも同じ割り引きを
  受ける（弱いものを数で狩るのが得になりすぎないように）。
- 割り引きは挟み込みで決めた（0 → 全滅、5 → 全周 14%、8 → 全周 32%、
  200/(n+1) → 全周 94%）。**8 を採用。** 直行 0% はどの通りでも保たれている。
- **シミュレータが 1 回で捕まえた。** 目で見ても「6 体は多いかな」までしか
  分からない類なので、測る側を先に作っておいた分がここで返ってきた。

## 2026-07-30（シナリオ六拍を画面へ）

- **生成しただけでは繋がっていない。** 六拍は `WorldMap.story` に在って検算も
  通っていたのに、一度も画面に出ていなかった。拠点地に着いたとき・城に着いたときに
  拍を出し、選んだ手を世界が覚え、終幕を戦記で回収するところまで繋いだ。
- **順番は 1 本道に保つ。** 3 拍目の土地へ先に着いても何も起きない。
  順序を崩すと話が読めなくなる。
- **物語の拍は見送れない。** イベントは「立ち去る」が一手だが、拍を飛ばせると
  話が飛ぶ。キャンセルでも同じ手（空の選択）で進める。
- **窓は増やさない。** イベントと同じ `EventView` を使い回した。出すものは同じ
  （何が起きているか / 選べる手 / その手で何が変わるか）で、違うのは「変わるもの」が
  数値か言葉かだけ。窓を 2 つにすると読み方も 2 つになる。
- **検算が空振りしていた。** 拍は座標ではなく `site_id`（"cave:2"）で土地に
  結ばれているのに、`beat.site.x/y` を見ていたので常に -1 で素通りしていた。
  **在るだけの検算は、無い検算より悪い**（通っているつもりになる）。
  id から位置を引く道を作って、本当に到達性を見るようにした。
- **はみ出しゲートが 12 件見つけ、そのうち 5 件は誤検出だった。** 窓が開く途中は
  `opening()` が縦に縮めた矩形を渡すので、その最中の文字は必ず外に出る。
  開き終わった窓は高さが整数になるので、端数のものを除けば見分けられる
  （`is_equal_approx` では 100.9996 を整数と見てしまうので、判定は自分で置く）。
  本物 7 件（戦闘の見出し、HUD、つよさ の装備行、名簿の最終行、熟練一覧、戦記の
  折り返し 2 件）は全部直して 0 件にした。

## 2026-07-30（造語を外部ファイルへ）

- **造語は見直しの対象なので、コードを触らずに差し替えられる状態にした。**
  `data/vocabulary.json` に集約（UI の呼び名 24 語、世界の前提 4 行、生物相 7 語、
  封の名の語彙表、町の名の語彙表）。`Terms` と `Lore` は残したので、
  **呼び出し側は 1 行も変わっていない**（`Terms.ECHO` のまま）。
- **`const` を `static var` にするのが鍵だった。** GDScript の const は
  関数呼び出しで初期化できないが、static var は式で初期化でき、
  参照の書き方（`Terms.ECHO`）も変わらない。`WorldMap.BIOMES` も同じ扱いにした。
- **既定値は右側に残す。** 語彙ファイルが無くても壊れた JSON でも動く。
  語彙が読めないだけでゲームが止まってはいけない。
- 差し替えが本当に効くことを確かめた（資源→アトリ、銀の砦→はじまりの館、
  雪原→こおりの野、封の接尾→かんぬき）。テストは「JSON を通っていること」を
  見るので、うっかり定数直書きへ戻したら落ちる。

- **開きかけの窓の誤検出は、縮めた本人に印を付けさせるのが確実だった。**
  端数（高さ・位置が整数でない）で見分けようとしたが、開く途中でちょうど
  整数寸法になる瞬間があって取りこぼした。`opening()` が返した矩形を
  そのまま除外対象に登録する形にしたら 0 件になった。
  **「見分ける」より「印を付ける」。**推測で判定を足すと必ず穴が残る。

## 2026-07-30（イベントの深さ）

- **危険は実際に振る。** 並べておいて起きないなら、それは危険ではなく飾りだった。
  45% で発火させ、起きたら支払いとして解決し、起きなければ「起きなかった」と
  はっきり出す（黙って通ると、危険があったことすら伝わらない）。
- **世界がプレイヤーを覚えている。** 選んだ手の tag を数えておき、後のイベントで
  同じ傾向が出たら一言添える。1 件ごとに独立していると、何件並べても
  「無関係な出来事の列」で終わる。
- **イベントの表層も AI に書かせた。** `WorldEventCatalog.facts_for_ai()` が
  id・選択肢・数値を落としてあるので、渡すのは表示枠と文字数上限だけ。
  返りは `QuestNarrativeText` が検算する。1 件ずつ順に頼むので待たせない。
- **返りの振り分けは「頼んだ側で印を立てる」。** 受け取る側で立てたら、返りが
  来る前に次を頼んだ瞬間に取り違えて、イベントの返事が封の名の処理へ流れ込んだ
  （`[AI:quest] 採用 0 項目` がその跡）。**印は依頼と同時に立てる。**

## 2026-07-30（オートの速度と呼び名）

- **オート中は間を詰める。** 自分で選んでいるときの間は読む時間として要るが、
  オートに任せているあいだは結果だけ見えればよい。メッセージの待ちを
  0.28 → 0.10、被弾の点滅を 0.3 → 0.12。**点滅も詰めないと 1 手番は縮まない。**
- **戦闘中の隅に長い説明を置かない。** 読まないのに場所を取るだけだった
  （「わけのわからない文言」）。呼び名を「守備重視 / 攻撃重視」に短くして、
  説明は隅から外した。呼び名で足りる。
- **作戦は戦闘をまたいで覚える。** 毎回 OFF から入れ直すのは、連戦のたびに
  同じ操作を繰り返させることになる。
- **枠の中でも隣の列に食い込む。** てんしょく の ★ を 4 つ並べると隣の職業名に
  届いていた。枠だけ見るゲートでは捕まらない ―― **「枠から出ない」と
  「重ならない」は別の条件**だったので、ゲートに文字どうしの重なり検出を足した。
  ★ の並べ方も「★4」に変えた（桁が増えても幅が変わらない）。
- 重なり検出は **1 フレームの中だけ**で見る。フレームをまたいで貯めると、
  同じ行に時間差で出る戦闘ログが全部「重なっている」ことになる。
  最初の窓が再登場したところをフレームの区切りにした。

### 反省

**要望を受けた項目（中断機能／デバッグ用の状態保存）を、実装せずに tasks.md から
消していた。** 「完遂」と報告した時点で、消えた項目は完了扱いになっていた。
**tasks.md から行を消してよいのは、archive に理由を書いたときだけ。**
消した記録が archive に無いものは、消してはいけない。

## 2026-07-30（恒久アップグレードに別の軸を足す）

- **4 種すべてが「立ち上がりが少し楽」だった。** 数は 4 つあっても軸が 1 つなので、
  選ぶ意味が無く「あるのと同じ」になっていた。**能力値に触れないという不変条件を
  守ったまま**、触れる軸を 4 つに分けた（立ち上がり / 情報 / 保険 / 値段）。
  - **封の言い伝え**（情報）… 出撃前から封の在り処が N 個分かる。町へ聞きに行く
    手間が減る。強くはならないが、道順の判断が変わる。
  - **命の綱**（保険）… 1 ラン に 1 度だけ全滅を肩代わりする。拠点の投資を
    「勝てるようになる」ではなく「もう一度立てる」へ効かせる。
  - **商いの目**（値段）… 品が安くなる。強くはならないが、買える品が増える。
- **並ぶ数字も割引後にする。** 買うときだけ安いと、表示が嘘になる。
- 7 種に増えて一覧が窓から溢れたので、列見出し（段 / ひつよう）と資源の説明を
  落として行に場所を回した。**数字の形（3/3、12）で読めるものに見出しは要らない。**

### 反省（2 度目）

**「アップグレードが貧相」も、実装せず tasks.md から消していた。** 中断機能と
同じ失敗を同じセッションで繰り返した。消した記録が archive に無いものは
消してはいけない、という規約を書いた直後に、もう 1 件見落としていた。
**残っている要望は、実装するか archive に理由を書くかの二択しかない。**

## 2026-07-30（中断機能とデバッグ用の状態保存）

- **世界を書き出さず、種から作り直す。** 決定性があるので `run_seed` 1 つで
  64x48 の地形・拠点地・封の配置・イベント・物語が完全に復元できる。
  書くのは「種から復元できないもの」だけ ―― どこに居て、何を解いて、何を持って
  いるか。丸ごと書くより安く、しかも**取りこぼしが構造的に起きにくい**
  （地形を書く形だと、項目を足すたびに書き出しへ追記し忘れる）。
- **ラン中の状態は `to_dict()` に混ぜない。** `PartyMember.to_dict()` は恒久データ
  専用で、中断用は `to_run_dict()` に分けた。混ぜると拠点のセーブに HP が
  入り込み、「失うもの / 残るもの」の境界が崩れる。
- **読んだら消す。** 同じ中断から二度始められると、実質セーブ&リセットになる。
- **復元が本当に一致するかはテストで見る。** 目で見て気づけるのは「同じ場所に
  立っている」だけで、封やイベントの進みが落ちていても画面では分からない。
  居場所・危険度・所持・封・物語・済んだイベント・えらび方の記憶・命の綱・HP、
  そして**世界の地形そのものの一致**を確かめる（247 項目へ）。
- タイトルは中断があると「つづきから」に変わる。**続きがあるのに新しく
  始めさせない**（うっかり上書きするのがいちばん痛い）。
- デバッグ用の名前付き保存は**中断と同じ書き出しを使い回す**（別書式にすると
  二重管理になる）。`--dev-save=名前` / `--dev-load=名前` で `user://dev/` へ。
- 引数の扱いで 2 度つまずいた。`--play=` が先に return して `--dev-save=` が
  読まれず、`--dev-load=` が return して `--play=` が始まらなかった。
  **先に全部の引数を見てから振り分ける。**

## 2026-07-30（封の番人・洞からの脱出・オートの重なり）

- **封を置いたのに守るものが居なかった。** ただし城の主と同格にすると、寄り道が
  本筋と同じ重さになって「寄るか急ぐか」が消える。そこで**別格**にした ――
  通常の敵を 1 体だけ、危険度 +2 で群れの割り引きを掛けずに置く。
  1 体なので範囲技が効かず、単体技の出番になる。テストで
  「通常の敵より硬く、城の主より弱い」を固定した（どちらかを外すと意味が変わる）。
- **シミュレータにも番人を通した。** ここを飛ばすと、封を集める線が実際より楽に
  測れてしまう。入れても帯は保たれた（直行 0% → 封 25% → 全周 35%）。
- **用が済んだ洞から出られるようにした。** ただし封が残っている洞では使わせない
  ―― それは番人を避ける近道になる。テストで 4 通り（世界の上／封が残る洞／
  解いた洞／封の無い洞）を固定した。
- **選べるのに見えない状態を作っていた。** メニューが 8 項目になって「ぬけだす」と
  「とじる」が窓の外に出ていた。**画面の外に出た行は、はみ出し検出でも捕まらない**
  （文字を描く座標が窓の中にないので、そもそも host が見つからない）。
  行送りを 24 → 17 に詰めた。**項目を足したら窓に収まるかを目で見る。**
- オート中は右上に作戦を出しているので、メッセージをその幅ぶん狭めて折り返す
  （切らずに置いたら 42px 重なっていた）。

## 2026-07-30（ゲートのフレーム区切りと、職業の似すぎ検出）

- **フレームの区切りを推測から明示に変えた。** 「最初の窓が再登場したら次の
  フレーム」は画面によって取りこぼし、戦記でテンプレート文と AI 文が重なっていると
  誤報した（実際は置き換えているので重なっていない）。各 View の `_draw()` 冒頭で
  `PixelUI.ui_frame()` を呼ぶ形にしたら 23 画面すべて 0 件になった。
  開きかけの窓と同じ結論 ―— **推測より印。**
- **職業の絵が似すぎているのを測れるようにした。** 15 枚を目で見比べるのは
  現実的でなく、とうぞくと忍者がほぼ同じ絵のまま通っていた。平均色の距離と
  塗られている面積の差の両方が近い組を出す。**描き換えるのはこちらの仕事では
  ない**（`docs/chara_image/` は外の領分）が、似ていることは測れる。
  検出は 6 組で、いちばん近いのが ninja–thief（距離 2.6）。指摘と一致した。

- **オート中の 1 戦を 25 秒超から平均 7〜9 秒へ。** 詰めるところは 3 つあった
  ―― メッセージの待ち（0.28→0.10）、被弾の点滅（0.3→0.12）、大技の閃光
  （0.55→0.18）。**1 つ詰めても残りが効いて縮まない**（待ちだけ詰めたときは
  点滅で、点滅も詰めたときは閃光が残っていた）。群れの回はまだ最長 17 秒で、
  ここから先は手番数そのものを減らす話になるので難度と一緒に測る。

## 2026-07-30（版を git の commit にする）

- **手で上げる番号は一度も上がらなかった。** `application/config/version` は
  0.1.0 のまま数十コミット進んでいた。人が覚えて上げる仕組みは忙しいときに
  必ず抜けるので、**放っておいても変わるもの**（git の commit）を identity にする。
- **実行時に git を叩かない。** 書き出した配布物に `.git` は無いので、
  エディタでだけ効く仕組みは不具合報告のときに役に立たない。
  `tools/stamp_version.py` が `data/build.json` へ写し、実行時はそれを読む。
- **刻印が無くても動く。** git が無い環境で配布物を作り直すことがあるので、
  無ければ番号だけを出す（テストで両方の道を見る）。
- **`*`（未コミットの変更を含む）を出すのが肝。** そのビルドは他の誰も
  再現できないので、不具合報告のときにここで切り分けられる。
- 刻印は `gen_assets.py` の頭でも走るので、普段は意識しなくてよい。
  書き出しの手順（README / AGENTS）にも先頭へ入れた。
- タイトル画面の隅にも出す。タイトルバーは配布物だと隠れることがある。

## 2026-07-30（A-1 またぐ物語のカタログ）

- **読込と検算だけの段。本体へはまだ繋がない。** 設計文書の導入順どおり。
  永続セーブを使う物語なので、一型で全経路を通してからカタログを広げる。
- 検算は設計文書の 5 点（型が 12 で ID 重複なし / 四段階が `phase_order` どおり /
  `placement` が語彙内 / 選択と結末が 3 件ずつ一対一 / `fallback_choice` が実在）。
  **一対一を見るのが効く** ―― 2 つの選択が同じ結末に落ちると、選んだ意味が消える。
  設計の 5 点の外だが、失敗継続の宛先も見る（外れると全滅時に何も起きない静かな穴）。
- **落ちる側を 7 通り試した。** 通す側だけ試すと、何でも通す検算でも緑になる。
- **壊した版は `duplicate(true)` ではなく JSON の読み直しで作る。**
  十二型は入れ子が深く、深い複製を取ろうとしたらエンジンごと落ちた。
  読み直すほうが安いし確実。
- `skin` は**候補の配列**（イベントカタログと同じ形）。AI へ渡すのは既定値 1 つ
  だけで、候補の全部は見せない ―― 選択肢を見せると、そこから選び直そうとして
  構造に手が伸びる。AI 無しでも `pick_skin()` で表示語が決まる（同じ種で同じ語）。

## 2026-07-31（A-2 またぐ物語の永続状態）

- **古いセーブが読めることが最優先。** 版を上げたときに遊んでいる人のデータを
  壊すのが、この手の追加でいちばんやりがちな事故。`cross_world` が無い版 2 の
  セーブは空の状態へ落ちる（形は `empty_cross_world()` 1 か所に置いた）。
  項目が欠けている場合も 1 つずつ既定で埋める ―― 部分的に古いセーブでも通る。
- **セーブへ長文を複製しない。** 結末は ID だけ残し、文章はカタログ側に置く。
  テストで「セーブに物語の本文が入らない」を固定した。
- 実セーブ（版 2、名簿 4 人・資源 899）は無事。保存の契機が来ていないので
  版はまだ 2 のままで、次に保存したときに 3 へ上がる。**確かめるために
  実データへ書き込むことはしない**（移行はユニットテストで見た）。

## 2026-07-31（A-3 またぐ物語の選出と進行）

- **全滅を挟んでも完了できることが主眼。** 永続なので、途中で止まるとセーブに
  死んだ型が残り続けて詰む。`setbacks` は結末の文脈に使うだけで、**打ち切る条件に
  しない**（打ち切ると、失敗したランが無かったことになる）。
- **乱数系列を用途ごとに分けた**（`meta_arc_select` / `_skin` / `_schedule`）。
  地形・敵編成・世界内六拍の乱数を消費しない ―― 混ざると、物語が始まっただけで
  世界の中身が変わる。
- **`phase_index` は表示または失敗継続が確定してから進める。** 表示前に進めると、
  落ちたときに段階だけ飛ぶ。
- **選ばずに終わっても結末は決まる**（`fallback_choice` へ落ちる）。画面を見ずに
  閉じたランで物語が宙に浮かない。
- 形（`empty_state`）は `CrossWorldArc` 側に置いた。`GameState` はオートロードで
  `--headless --script` から触れないので、**テストしたいものは静的側に置く**。

### 事故

`game_state.gd` から `empty_cross_world` の中身を移すとき、置換の終端を
`


` で取ったせいで**後ろの変数宣言 76 行をまとめて削った**。
`--import` が 177 件のエラーを出して気づいた。git から取り戻したが、今度は
二重定義になり、重複を落として復旧。**範囲を「次の空行 3 つ」のような曖昧な印で
切らない。** 関数名や見出しのような一意の印で挟む。

## 2026-07-31（封の番人が倒れない不具合）

- **「番人を倒した」印を立てる行が入っていなかった。** 階段を踏むたびに番人が
  出続けて、封が永久に解けない状態だった。フラグを false にする行はあるのに
  true にする行が無い、という抜け方で、**コードを読んでも気づきにくい**
  （`grep _guardian_beaten` で「代入が false しかない」と分かって初めて見えた）。
- **解除まで通ることをテストで固定した。** 倒す前は残っている / 倒したら解ける /
  残りが 1 つ減る / 二度は解けない。フラグ 1 つの抜けは目で追えないので、
  **状態が進むことそのものを assert する。**
- 全滅・逃走のときも番人の戦闘印を落とす（落とし忘れると、次の戦闘が
  番人扱いになって同じ穴が別の形で出る）。

## 2026-07-31（A-3b またぐ物語を画面へ出す）

- 出す場所は 3 つ。**ラン結果の段階は戦記へ**（拠点の通知だと、ラン終了直後の
  画面をまたいで消える）、拠点の段階は通知へ、**最後の段階だけ選ばせる**。
- 選択は `EventView` を使い回した。世界内の六拍と同じ作りで、**窓を増やさない**。
  またぐ物語も見送れない（見送ると既定の手で閉じる）。
- **全滅は物語を打ち切らず、失敗として書き留めるだけ。** `_finish_run` の
  いちばん最初に置いた（`end_run()` より先に書かないと、その回の失敗が
  結末の文脈に入らない）。
- **城の前で足踏みしていた。** 封が残って入れないとき、城のマスに立ったままに
  していたので、一歩動くたびにまた扉を叩いていた。町で直したのとまったく同じ穴で、
  自動プレイが 60 秒で戦闘 1 回まで落ちて気づいた（直して 8 回）。
  **「入れない場所から追い返すときは 1 マス外へ出す」を作法として揃える。**

## 2026-07-31（C-7 候補・生成物・実行時参照の三段検査）

- **「画像はあるが一度も出ない」を機械に見させる。** このリポジトリで何度も
  起きた失敗で、どれも**動くので気づけない**（NPC の絵があるのに主人公の絵を
  流用、エフェクトを生成したのに参照が無い、背景を描いたのに階調のまま）。
- 三段を数える ―― 候補（`docs/chara_image/`）/ 生成物（`assets/`）/ 参照（`src/` と
  `data/`）。生成済みなのに参照 0 なら落ちる。候補があるだけのものは報告のみ
  （取り込む判断は人がする）。
- **「組み立てているから全部参照済み」と数えてはいけない。** 最初の版は
  `npc_%s.png` という書き方があるだけで 17 種すべてを参照済みにしていた。
  実際に繋がっていたのは 6 種だけ。**接頭辞を除いた名前が表に載っているか**で
  数えるようにしたら実態が出た。表は `src/` だけでなく `data/` にもある
  （職業 id は `data/jobs.json`）。
- 部分接続は落とさないが数字で出す。「絵はあるのに半分しか出ない」の温床なので、
  並んでいれば気づける。
- 実測: 職業 15/15、**NPC 6/17**、イベント演出 1/4。敵 18・戦闘背景 6・
  FX 12・トランジション 4 は候補があるだけで未取り込み。

## 2026-07-31（C-1 町の外の灰色 / B-1 NPC 全接続 / 撮影の競合）

- **地図の外を先に塗る。** 町は 20〜30 タイルで画面（32）より狭く、余った側に
  前の場面の絵が残っていた。町を 32 タイルへ水増しすると歩く距離が増えるだけ
  なので塗りで解く。あわせて**画面より狭い地図は中央へ寄せる**
  （端に寄せると片側だけ余白になり「途中で切れている」ように見える）。
- **`--shot=` がタイトルを撮っていたのは私が入れた行のせいだった。**
  `--dev-load` 対応で `_handle_debug_args()` の後に `_enter_title()` を置いたが、
  `_capture()` は `await` を含むので呼んだ直後に制御が戻る。そこでタイトルを
  出すと撮ろうとした画面を上書きする。**引数を捌いたかどうかを返す**形に直した。
- **NPC を 6/17 → 17/17。** もとは 4 役を主人公の絵に割り当てていた。
  **役の名前をそのまま絵の名前にした**（`npc_<役>.png`）ので、対応表を持たない。
  表を挟むと絵を足すたびに表を直すことになり、また「絵はあるのに出ない」が起きる。
- **役ごとに言うことを変えた。** 全員が世間話をすると町が背景になる。
  鍛冶は道具、船頭は水路、難民は外の世界 ―― その役だから言えることを書く。
  顔ぶれは**必ず居る役（宿・店・物知り）→ 土地に合う役 → 残り**の順で選ぶ。
  用が足せることを先に保証してから顔を振る。
- **敵の絵は誤報だった。** `check_assets.py` が接頭辞で探していたが、敵の絵は
  接頭辞を持たない（`assets/sprites/<敵id>.png`）。36/36 で繋がっていた。
  **嘘をつく関門は無いより悪い**ので、`data/monsters.json` の名前と突き合わせる
  形に直した。

## 2026-07-31（C-3 敵を整数倍で見せる）

- **等倍で出していたので顔も装備も主の格も潰れていた。** 48x48 や 64x64 を
  512x320 にそのまま置くと、SFC 期の敵より小さい。最近傍の 2 倍にした。
  **半端な倍率は使わない**（ドットが滲む）。
- **位置・影・カーソル・状態・数字を、すべて表示矩形から計算し直した。**
  絵の元寸から計算していると、倍率を変えるたびに全部がずれる。
  1 行に何体置けるかも表示寸法から決める（いちばん広い絵に合わせる）。
- 48px が主体で 64px は単体で出る主だけなので、2 倍でも 3 体並びが収まる。
  6 体は 2 段。奥の段は上へ逃がして右へずらす（真上だと 1 体に見える）。
- **`--shot=boss` が世界を撮っていた。** `_enter_world()` の暗転と主戦の閃光が
  かち合っていたので、撮影では世界へ入り直さず戦闘だけを立てる。

## 2026-07-31（C-4 戦闘背景を生物相へ接続）

- **2 倍にした敵が広い空白に浮いて見えた**（C-3 と対）。紺の階調だけでは
  「場所」にならないので、512x176 の背景 6 枚を生物相へ割り当てた。
- **階調背景は消さずに残す。** 絵が 1 枚も無くても遊べること、が取り込みの前提。
  `backdrop_of()` が無ければ null を返し、階調へ落ちる。
- **城の中だけ生物相ではなく城の絵**（帝国工廠）にした。主戦だけ別の場所に
  見えるのが効く。土地の名と混ざらないよう `@castle` という鍵で分けた。
- 撮影が城に入っていなかったので背景が草原のままだった。**撮影も実プレイと
  同じ道を通す**（`enter_site()` してから戦闘を立てる）。

## 2026-07-31（C-6 イベント演出 4 種を場面へ）

- 192x48 のアトラスを 4 コマで再生する部品（`EventEffect`）を作り、節目へ置いた
  ―― 世界の門（ランの始まり）、封印解除、帝国警報（主戦へ入る）、年代記（戦記）。
- **失敗しても進行を変えない。** 絵が無ければ何も出さずに終わるので、呼び出し側は
  成否を気にしなくてよい。乱数にもセーブにもイベントの結果にも触らない。
- **入力を止めない。** 上に重ねるだけで押した手は通る（演出のために待たされるのが
  いちばん嫌われる）。0.4 秒で終わり、終わりぎわだけ薄くする
  （ぷつりと消えると「絵が抜けた」ように見える）。
- 倍率は敵と同じく整数倍（2 倍）。半端な倍率はドットが滲む。
- **撮影が演出の終わったあとを撮っていた。** 演出は 0.4 秒で終わるのに撮影は
  0.84 秒後だった。撮る対象の寿命に合わせて待ちを変える。

## 2026-07-31（B-2 戦闘エフェクト 12 種）

- 32x32 の 4 コマを、被弾した相手の位置に出す。**属性が最優先**（炎の技は炎に
  見えるべき）で、無ければ系統、それでも足りないもの（突き・銃・眠り・毒）は
  技ごとに指定。対応が無ければ空を返し、従来の点滅と数字だけになる。
- **位置は `_draw_enemies()` と同じ式から取る。** 別に計算すると、並びを変えた
  ときに絵だけ元の場所に残る。
- **対応表は画面ではなく静的クラス（`BattleFx`）へ置いた。** Node に属する
  静的関数は `--headless --script` から呼べず、テストが
  「Nonexistent function」で落ちた。**テストしたいものは静的側に置く**
  （`CrossWorldArc` で学んだのと同じ）。
- **割り当て先の絵が実在するかもテストで見る。** 名前を書き間違えても
  「黙って何も出ない」で通ってしまうため。40 技すべてに絵が付いた。
- 場面の演出（48px）と戦闘の絵（32px）でコマの大きさが違うので、
  `EventEffect` が名前の頭（`fx_`）で置き場とコマ寸を切り替える。

## 2026-07-31（C-5 職業画像の拡大 / C-2 自動プレイの町滞在と集計）

済んだ項目:

- [x] **[Codex追記 C-2] 自動プレイを町の中に滞在させ、監査値を正しくする。**
      **Gate（後追い）:** `--play=150` の集計に「町に入った N 回（滞在 M 秒）」が
      出ることを確認した。滞在が 0.3 秒だった頃の値には戻っていない。
      今回の町滞在は108.9〜109.2秒の0.3秒だけで、NPCを一人も撮れなかった。
      町では最低2人に話し、宿か店を試してから出る経路を持たせる。
      併せて、危険度10へ到達したのに「最も危険: 1」と出る集計と、
      世界↔町の2状態反復を検出できない詰まり判定を直す。

- [x] **[Codex追記 C-5] 職業画像を拠点と職業選択で鑑賞できる大きさにする。**
      現在の48x64表示では角、仮面、有機襟がほぼ読めない。
      72x96の最近傍3倍を基準に人物領域を確保し、全15職を同じ枠で撮る。
      （`--shot=` が暗転待ちの競合でタイトルを撮る問題は解決済み）


- **立ち絵を 2 倍から 3 倍（72x96）へ。** 角・仮面・有機的な襟が読めなかった。
  職業の描き分けはシルエットで作っているので、小さいと**その努力が全部消える**。
- **自動プレイが町を素通りしていた**（滞在 0.3 秒、NPC を一人も踏まない）。
  町では 12 秒ねばって人にぶつかり、店を試してから出口へ向かうようにした
  （酔歩では閉じた広場から出られないので、出るときは経路に従う）。
  実測 0 回 → 6 回。
- **「最も危険 1」の集計が 2 度目の同じ抜けだった。** 表示を「危険度」に変えたのに
  「地下」を探したままで、危険度 9 まで行っても 1 と出ていた。
  **文字列を当てにした集計は、表示を変えたら必ず一緒に直す。**
- **2 状態の往復を詰まりとして検出する。** 「同じ画面に貼り付く」だけを見ていたので、
  世界と町を出入りし続ける足踏みは素通りしていた（実際に起きた）。
  ただし**戦闘を含む往復は正常**（歩く↔戦うはゲームが動いている姿）。
  そこを除かないと本物の足踏みが埋もれる。

## 2026-07-31（tasks.md の行数上限を外す）

- **上限が足枷になっていた。** 200 行以内という決まりは「読まれない一覧は
  棚卸しされない」という理屈で置いたが、指摘が多い時期には**「積むより先に
  消す」圧力**として働く。実際、要望（中断機能・アップグレードの貧相さ）を
  実装しないまま tasks.md から消す事故を 2 度起こした。
- **数えるものを変えた。** 行数ではなく「`[x]` の行が残っていないか」を見る。
  済んだ項目は `[x]` を付けて控えへ移すので、tasks.md に `[x]` が残っていたら
  それは移し忘れ。**積むのは自由、消すのは archive 経由**。
- 上限を外す作業で、置換の範囲を広く取りすぎて前の 3 関数を巻き込んで消した。
  git から取り戻した。**範囲を「直前の見出しコメントまで」のような相対の印で
  切らない**（A-3 でも同じことをやっている）。

## 2026-07-31（遭遇の閃光をモザイクへ）

- [x] **遭遇時の閃光が強すぎる。往年の RPG のトランジションを調べて学ぶ。**
      調べたことは `docs/screen_transition_design.md` に書いた。要点は
      **時代の取り違え**だったこと ―― SFC の明度レジスタ（`$2100`）は
      既定が最大輝度で、**暗くする方向にしか無い**。白く飛ばすのは
      ハードの機能ではなく、色演算で固定色を足す「わざわざやる」演出だった。
      対して FC は全画面演出がパレット差し替えしか持たず、必然的に閃光へ寄る。
      **白く 2 回瞬く画面が「FC っぽい」のは趣味の問題ではなく出自の問題**で、
      最初の指摘（「FC な印象」）は演出の言語そのものを突いていた。
      採ったのはモザイク（`$2106` 相当、`src/ui/mosaic.gdshader`）。
      実機の第一語彙であること、**画面の中身を残したまま覆う**ので
      歩いていた地形が粗く溶けて戦闘へ繋がること（暗転や閃光が前後を切るのに対し、
      モザイクは繋ぐ）、そして眩しくないこと。粒は 1〜16 の整数段、
      明るさは 16 段に量子化した（滑らかに補間すると現代のフェードになる）。
      入り 0.30 秒・戻り 0.20 秒。**強い明滅は入れていない。**
      白が戻らないよう `_test_no_white_flash()` で加算方向を禁じ、
      `--shot=transition` で崩れている途中を撮れるようにした。
      なお `candidate_transition_*.png`（アイリス等）はこの件では使っていない。
      あれは場面転換用で、遭遇はモザイク 1 本のほうが速くて筋が通る。B-3 に残す。

- **C-5 の移し忘れを拾った。** 上（`af350ff`）で済んでいたのに印だけ tasks.md に
  残っていたのを、衛生の関門（`_test_docs_hygiene`）が拾った。
  **項目としては上で閉じているので、ここでは二重に記帳しない**
  （台帳の関門が「控えに 2 回出てくる ID」として落とす）。

## 2026-07-31（1 戦が長い ―― 測ったら前提が外れた）

- [x] **敵 6 体の 1 戦がまだ最長 17 秒。**
      Codex の「6体戦だけと決めずに測れ」が正しかった。**敵の数の問題ではない。**

      *測ったこと*（`balance.gd` に敵の数ごとの手番・行数・秒と、危険度ごとの
      長い戦の割合を出させた）:

      * 敵 1 体でも最長 11.9 秒。長い戦はどの体数にも同じように出る。
      * 長い戦が固まるのは**危険度に対してレベルが足りていない時**。
        直行（洞を飛ばす）は危険度 6〜8 で 17〜25%、封だけ狙いは危険度 10 で 20%。
      * **1 戦の長さを決めているのは手番数ではなく行数だった。**
        点滅（0.12）と閃光（0.18）は `_process` で待ちと**並行**に減るので
        秒を足していない。効いていたのは「1 行ごとに 0.10 秒」だけ。
        手番数から 0.40 秒/手番と見積もっていたが、根拠が無かった。

      *直したこと*:

      1. **削るのではなく束ねた。** 窓は 3 行ぶんあるので、オート中は
         窓 1 枚ぶんをまとめて出して 1 回だけ待つ（`AUTO_WINDOW_DELAY 0.16`）。
         待ちの回数が 1/3 になるぶん 1 回を長くできるので、
         **全体は約 28% 速く、1 行あたりは今より長く読める**。
         遅延をこれ以上削るのは筋が悪い（0 にすると何が起きたか読めない）ので、
         この方向は採らなかった。
      2. **毒を受けた者が行動せずに手番を終えていた。** `_begin_turn()` が
         毒の行を出したところで `return` していた。しかも Sim は行動させて
         いたので、**測っている強さと遊べる強さが別物**になっていた。
         行を読ませたあと同じ者の行動へ戻すようにした（`_resume_actor`）。

      *結果*（同じシードの同じ戦闘を新旧の式で並べた。約 4,800 戦）:

          敵 1 体  平均 1.4→1.0 秒   最長 11.9→ 9.3 秒
          敵 4 体  平均 3.8→2.7 秒   最長  9.6→ 7.2 秒
          敵 6 体  平均 5.5→3.9 秒   最長 11.3→ 8.3 秒

      **Gate:** `godot --headless --script tests/balance.gd -- --runs=200`
      → 判定「妥当な帯（全周 42% / 封だけ狙い 19%）」で難度は動いていない。
      `tests/test_core.gd` 346 / 0、`tools/check_ui.py` 23 画面はみ出しなし。
      残る尾（最長 9 秒台）は**手番そのものを減らす**方向でしか縮まず、
      それは難度に直に効くので**今回は引かなかった**。

- [x] **関門と測定が遅すぎた／嘘をついた。**
      `check_ui.py` が 23 画面を直列に立てて数分かかり、しかも一斉起動で
      稀に落ちた回を「画面が開かなかった」と報告していた（jobmenu が単独では
      通るのに関門だけ赤い）。**嘘をつく関門は無いより悪い。**
      並列（CPU の半分）＋失敗時 1 回だけ再試行にして、**23 画面 10.7 秒**。
      **Gate:** `python tools/check_ui.py` が 23 画面すべて OK で 10.7 秒。
      `balance.gd` は勝率の判定に 200 ラン要るが、1 戦の長さは 40 ランでも
      数百戦になる。**見たいものによって必要な試行数が違う**ことを定数に書いた。

## 2026-07-31（D-4 関門の偽陽性 ／ B-3 画面トランジション）

- [x] **[棚卸再開 D-4] アセット検査の偽陽性をなくす。**
      直したのは 3 つ。
      1. **1 枚でも未接続なら落とす。** 前は「その分類が全部未接続」のときしか
         落とさず、4 枚中 1 枚でも繋がっていれば「生成済みはすべて参照されている」
         と表示していた。
      2. **保留にタスク ID を必須にした。** 前は分類ごとの保留だったので、
         その分類の絵は何枚増えても素通りだった。いまは生成物の名前ごとに、
         理由へ ID（`B-3` など）が無ければ落ちる。
      3. **トランジションの接頭辞が実物と違っていた。** `transition_` で
         探していたが生成物は `iris_gate.png` のように接頭辞を持たない。
         そのため「生成 0 / 候補があるが取り込んでいない」と誤報していた ――
         実際は 4 枚とも生成済みで、未接続なだけだった。
         **この関門が嘘をついたのはこれで 3 度目。**
      **Gate:** `python tools/check_assets.py --selftest` を足した。
      判定を純関数（`judge`）に切り出し、実データも作り物も同じ関数へ通す。
      未接続 1 枚 → 1、全部保留 → 0、未接続なし → 0、ID の無い保留 → 1 の
      4 件を確認（`--selftest` 通過、本番も 0）。**保留は現在ゼロ。**

- [x] **B-3. 画面トランジション 4 種を取り込む。**
      場面ごとに割り当てた ―― アイリス＝拠点と世界の行き来（門をくぐる）、
      頁めくり＝戦記と結果（記録に移る）、歯車＝城と主戦（帝国の側へ入る）、
      画素の増殖＝町や洞の出入り。
      **戦闘からの復帰には付けていない。** そこは `_set_mode` を直に通るし、
      1 ラン中に何十回も通るので、演出を挟むと目に見えて重くなる。

      *つまずき 2 つ*:

      * **覆いを `ScreenTransition`（ColorRect）の子にしていた。** 普段
        `visible = false` なので子ごと消え、一度も描かれていなかった。
        しかも画面に写っていた銀の輪を「覆いが出ている」と見間違えた ――
        あれは世界地図の門だった。**兄弟として置くのが正しい。**
      * **輝度マスクを噛ませたのが余計だった。** 明るいところを穴と
        みなしたら、銀の刃のハイライトまで抜けて刃の中に地形が透けた。
        測ったらアトラスは**透明度で覆いを表していた**（不透明の割合が
        コマ 1 の 44% からコマ 8 の 100% まで増える）ので、そのまま重ねればよい。
        残したのは締め（`seal`）だけ ―― 覆いきった一瞬が無いと、
        その裏で画面を差し替えたのが見えてしまう。

      **Gate:** `--shot=cover` で覆いきる途中を撮って目視。
      `check_assets.py` のトランジションが 生成 4 / 参照 4 になり、保留が空。
      `test_core.gd` 353 / 0、`check_ui.py` 27 画面はみ出しなし、
      `--play=120` 詰まりなし。

## 2026-07-31（D-1 任意イベント品質Gate）

- [x] **[棚卸再開 D-1] 表示だけで終わるイベント効果11種を実装する。**
      既知トークンなら通す検査を廃止し、**実際のラン状態が変わらなければ落ちる**
      Gateへ置き換えた。道の安全・近道・地図・生物相、主の情報と弱体、町と宿の
      貸し借り、一時援護を本番経路へ接続した。`encounter_up/down` と店の品数補正も、
      値だけ保持して利用されていなかったため遭遇判定・店在庫へ接続した。

      `elite_fight` は表示だけ手強く、実際には通常遭遇を呼んでいたため、同じ編成を
      25%強化する強敵戦にした。危険側の戦闘は抽選で発火した場合だけ予約する。
      効果ゼロの「触れずに去る」「取引しない」は済み扱いにせず、再訪できる
      `defer` として明示した。結果は専用の大きな窓で、代償・危険・報酬を読むまで
      閉じない。

      **Gate:** `godot --headless --script res://tests/test_world_events.gd`
      → 83成功 / 0失敗。64型・192選択肢・全トークンの状態差、強敵戦、主戦補正、
      宿での成功／拒否、失う物が無い危険の負傷fallbackを検査した。
      効果ゼロを完了扱いするfixtureと、allowedへ足しただけの未知トークンfixtureは
      どちらも検査器が拒否する。
      `-- --fixture=inert` は意図した違反を報告して終了コード1になることも確認した。

      `test_core.gd` 365成功 / 0失敗。イベント効果・既知の封・変化した生物相が
      中断再開で戻ることも確認した。`--shot=event --ui-check` と
      `--shot=event_outcome --ui-check` は見切れなしで両方を目視。
      新規データの `--play=45` はイベントを2件通過して詰まりなし。
      数値確認 `balance.gd -- --runs=40` は「妥当な帯」
      （封だけ17% / 全周45%）を維持した。

## 2026-07-31（セーブ消去）

- [x] **タイトル設定へ、プレイヤー進行の全消去を追加する。**
      `save.json` だけでなく、一世代控え・書きかけ・中断も同じ操作で消す。
      音量とキー割り当て、および開発用スナップショットは別の寿命なので残す。
      ファイル消去後はオートロード上の戦績・資源・熟練・名簿・またぐ物語も
      初期状態へ戻し、次の開始ではプロローグをもう一度表示する。

      消去はタイトルから開いた設定でだけ有効。確認窓は「やめる」を既定位置にし、
      決定連打では消えない。ラン中のAPI呼び出しも拒否し、終了時の自動保存で
      消したはずの進行が復活する経路を塞いだ。

      **Gate:** `test_core.gd` 378成功 / 0失敗。本体・控え・書きかけ・中断の削除、
      ラン中拒否、恒久状態の初期化、初期4人とプロローグへの復帰を検査した。
      `--shot=settings --ui-check` と `--shot=save_erase --ui-check` は見切れなし。
      目視でも通常設定5行と確認窓の収まり、既定カーソル「やめる」を確認した。

      全体Gateで、入口を南中央へ固定した後の一部の町でNPCが必須通路を塞ぐ回帰も
      発見した。町人を1人置くたび宿・店・出口への到達性を再検算するよう修正し、
      同じ `test_core.gd` で到達性を回復した。

## 2026-07-31（C-9 拾ったら装備 ／ C-10 さいきょう装備 ／ 町の出入口を固定）

- [x] **[Codex追記 C-9] 装備入手直後に「今そうびする」を選べるようにする。**
      集約は `GameState.gear_gained` の 1 本にした。宝箱・イベント・店の
      どれも `add_gear()` を通るので、**経路が増えても聞き忘れが出ない**
      （経路ごとに聞いていたら必ず取りこぼす）。初期装備の支給と開発用の付与は
      `announce=false` で黙らせる。
      画面は `GearOfferView`。仲間ごとに**いまの値 → 着けた後の値**と、
      外れる装備を出す。1 行に名前と差分を並べたら、能力が 3 つ動く装備で
      名前が「ア…」に詰まった（関門が拾った）ので、差分は 2 行目へ下ろした。
      `Mode.GEAR` を足して**開いているあいだは歩けない**ようにしてある
      （選んでいる最中に遭遇や階段が割り込まない）。断っても手持ちに残る。
      表示語は `Terms`（`data/vocabulary.json` に無くても既定へ落ちる）。
      **Gate:** `--shot=gearoffer` を `check_ui.py` の 30 画面へ追加。はみ出し・詰め 0。

- [x] **[Codex追記 C-10] 「さいきょう装備」を共通処理として実行できるようにする。**
      `BestGear`（`src/game/best_gear.gd`）1 本。装備画面の「さいきょう」からも、
      `AutoPlay` からも**同じここを呼ぶ** ―― 別々に書くと、自動プレイが人と違う
      装備で走り、測っている強さと遊べる強さがずれる（毒で手番が消えていたのと
      同じ事故）。
      重みは職業の `growth` から引く。15 職ぶんの表を手で持つと、職業を足した
      ときに必ず片方だけ古くなる。倍率は装備の値幅を揃えるためのもので、
      これが無いと `hp` が常に勝って `guard_ring` が全職の最適解になった。
      行動コストは負が良いので符号を反転し、速さを伸ばす職ほど重く見る。
      **乱数は引かない**（装備の割り当てで乱数列を進めたくない）。同点では
      替えない。手持ち 1 個を 2 人へ配らない。優先度は仲間の並び順で、
      全体最適ではないが結果が読めて説明できる。
      **Gate:** 代表 4 職で確認 ―― 戦士に `いくさ斧`＋`くさりかたびら`、
      魔法使いに `かしの杖`＋`きぬのローブ`、僧侶に `ほのおの短刀`。

- [x] **町の出入口を南辺中央へ固定した（C-8 の一部）。**
      四辺から乱数で選んでいたが、**変える理由が無かった。** 害だけがあった ――
      入った直後にどちらを向けばいいか分からない、出たつもりが別の辺で世界地図の
      上を行き来する（自動プレイが世界↔町を 8 回続けて往復した）、
      「戻る場所」を覚えられないので町が土地として頭に残らない。
      町ごとの違いは**入口より先**の街路と地区で作る。
      **Gate:** `test_core.gd` の「町の宿・店・出口に必ず辿り着ける」が 5 シードで通る。

- [x] **自動プレイが町から出られなかった。**
      滞在時間が切れた回に `_town_entered` を戻していたので、出口へ 1 歩進むと
      次の呼び出しでまた滞在が始まっていた。**出口が 1 歩より遠い町からは
      抜けられない。** 実際に 90 秒とどまった。出ると決めたら出きるまで
      出口へ向かう印（`_town_leaving`）を持たせた。
      **Gate:** `--play=150` で町に 18 回入り、滞在 11 秒、以後 90 秒の張り付きは出ない。

- [x] **UiPanel が黙って詰めていた／`wrap` が幅を超えていた。**
      `draw_text` 側は詰めを記録するのに、列幅で詰めたぶんはどこにも出ていなかった。
      `wrap()` は行頭に置けない字を前の行へ送り返すので指定幅をわずかに超え、
      そのぶんが `line()` で詰まって鳴った ―― 折り返し幅を 1 文字ぶん狭く取る。
      熟練の「429 きわめた」は、極めたあとの累計に使い道が無いので数を落とした。
      **Gate:** `check_ui.py` の詰め報告が 0 件（導入直後は 2 件出ていた）。

## 2026-07-31（通常遭遇の最低間隔）

- [x] **[Codex追記] 通常遭遇後の6歩を完全な安全期間にする。**
      従来の `MIN_SAFE_STEPS = 9` は実歩数ではなく地形の遭遇重みだった。
      そのため重み3の森では3歩で安全期間が終わり、連戦が起こり得た。
      実歩数を別に数え、**遭遇後の1～6歩目は抽選そのものを行わず、7歩目から**
      従来の重み・イベント補正による抽選を再開する。町・洞・イベント地点へ
      入ったときも両方のカウンターを戻すため、場面切替直後の事故遭遇も防ぐ。
      安全期間には乱数を消費せず、同じシードの編成・行動列を保つ。

      **Gate:** `test_core.gd` 382成功 / 0失敗。全32シードで1～6歩目が安全、
      その間に遭遇乱数を消費しないこと、7歩目に抽選が戻ること、実歩数を
      満たしても従来の重み下限を迂回しないことを検査した。
      `test_world_events.gd` 83成功 / 0失敗で、安全／危険イベント補正も
      安全期間後には実判定へ届く。

      数値変更後に `balance.gd -- --runs=200` を再測定し「妥当な帯」
      （封だけ狙い: 主到達32% / 勝利18%、全周: 主到達44% / 勝利36%）。
      実画面の75秒自動プレイでは通常遭遇1件を記録し、直前の間隔は23実歩だった。
      この試走では自動操縦が同じ町へ再入場する既存挙動も確認したため、
      最低間隔の保証は試走件数ではなく上記の全シード Gate で固定する。

## 2026-07-31（G-1 窓の割り付けと単独テスト）

- [x] **[Claude担当 G-1] 汎用Windowの配置基盤と単独テストを追加する。**
      **`UiWindowFrame` / `UiWindow` は作らなかった。** 指定はその 2 型を新規に
      足す形だったが、同じ役目の `UiPanel` が既にあり、**同じ役目の型を 2 つ置くと
      片方だけ直したずれが必ず出る**（Sim と実装で 2 度やっている）。
      欠けていたのは型ではなく**単独テスト**のほうだったので、そこを足した。

      `tests/test_window_system.gd`（18 件）。画面を撮らずに検査する ――
      `check_ui.py` は 31 画面を立てるので 1 分近くかかるうえ、
      「たまたま今の文言だと収まっている」ことしか言えない。ここは
      **わざと壊した入力**を渡して、詰まる・捨てる・数えるが起きることを見る。

      見ているもの: 幅を 3px 超えたら詰める／収まる幅では詰めない／詰めた印が
      `…` で終わる／詰めた結果が幅に収まる／詰めたことを記録する／左右が
      ぶつかったら左を詰めて右（数値）を守る／高さが足りない行を外へ描かず
      数える／列が隣へ食い込まない／折り返した行が 1 行も詰まらない／
      添え物は 12px のまま・本文は 14px 以上・漢字の検出そのもの（D-5 の一部）。

      そのために `UiPanel` の**測る部分と描く部分を分けた** ―― `_canvas` が
      null なら描かずに割り付けだけ進む。窓を立てずに検査できる。

      **Gate:** `_shrink()` から詰めを外して**わざと壊すと 5 件が落ちる**ことを
      確認（正常時は 18/18、終了コード 0）。

## 2026-07-31（G-2 / G-3 全 View を UiPanel へ移す）

- [x] **[Claude担当 G-2] 出店4窓と通知を試験移行する。**（下の G-3 と同じ Gate で確認）
- [x] **[Claude担当 G-3] 残り7Viewを小さい順に移行してUI Gateへ統合する。**

      **生の `draw_text` は 151 → 18。** 残る 18 件はいずれも境界が保証済み ――
      `UiPanel` 自身の実装 5、中央寄せ・右寄せの原始関数経由 8（どちらも
      置く前に幅を見る）、窓の外の短い固定語 5（「順」など）。

      *移行して分かったこと*。**溢れの原因はほぼ 1 つの型だった** ――
      「ラベルから固定のオフセットに値を置く」。長いラベルが必ず値へ食い込む。
      実際に出た指摘（つよさで重なる／銀の砦ではみ出る／転職ではみ出る）は
      全部これで、`row()` が右（値）を守って左を詰めれば起きない。
      同じ形が 12 か所にあった ―― 熟練一覧 3 列、技一覧 3 列、職業一覧 2 列と
      4 列、味方欄 4 人、店の品書き、アップグレード 3 列、装備、設定 2 か所。

      *もう 1 つの型*が「折り返し幅を数字で書く」。470 / 468 / 464 / 218 が
      散らばっていて、**窓の寸法を変えた瞬間に古くなる**。`paragraph()` は
      窓の内側から幅を取るので、その数字が要らなくなる。

      途中で見つけた副産物: 行動順の札は `_short_name()` が**文字数**で詰めて
      いたが、文字数と幅は比例しない（「あ」と「i」で倍違う）。
      中央寄せは幅を測ってから置き場所を決めるので `draw_text` の自動収めでは
      間に合わず、`draw_text_center()` を足して**先に詰めてから測る**ようにした。

      **Gate:** `check_ui.py` 31 画面はみ出し・詰めなし、`test_core.gd` 414/0、
      `test_window_system.gd` 18/0。代表画面（つよさ・転職・出撃・編成・
      アップグレード・店・イベント・戦闘・序章・タイトル・拾った装備）を目視。

## 2026-07-31（D-5 漢字サイズを関門にする）

- [x] **[棚卸再開 D-5] 表示語と漢字サイズの規約をGateにする。**
      **文字列を grep するだけでは足りなかった。** ソースの literal を
      `SIZE_SUB` の行で探すと 1 件しか出ないが、実際には 13 件あった ――
      品名も技名も職業名も説明も `data/*.json` 側にあり、`Terms` の差し替えでも
      入ってくる。**描いた瞬間に見る**のが確実なので、`draw_text` が
      「14px 未満 かつ 漢字を含む」を記録し、`--ui-check` と `check_ui.py` が
      違反として扱う。詰めの記録と同じ仕組み。

      直したのは 13 件。大半は**意味を運ぶ文字を 12px で描いていた**もの ――
      装備と道具と技の説明、装備の品名、能力の差分、代価と報酬、
      「しょくぎょうの 熟練」、序章の promptなど。`SIZE_SUB` は
      **かなと数字だけ**という決めごとに戻した。

      イベントの「はらう / あぶない」は左右に割っていたが、半分の幅（242px）に
      14px の漢字が入らない。12px へ下げると潰れるので、**行で分けた** ――
      縦に 1 行使うほうが読める。

      NPC の台詞に残っていた `north` も「きた」に直した。

      **Gate:** `check_ui.py` 31 画面で 0 件（導入時は 13 件を検出した）。
      検出そのものの試験は `test_window_system.gd` に置いた ――
      漢字を含む語を見つけ、かなと数字だけの語は見逃すことを確認する。
      なお「UIラベルを Terms 経由にする」ほうは**未着手**。画面に直書きした
      短い操作案内（「Ｚで きめる」など）が残っている。別途 D-7 として積む。

## 2026-07-31（D-3 LLM の接続点を 1 か所へ）

- [x] **[棚卸再開 D-3] LLM接続点を1か所へ戻す。**
      **「戦記だけにする」とは読まなかった。** クエストとイベントの文を AI に
      作らせるのはコンセプトそのもの（世界ごとに変わるものを AI に、
      変わらないものを Script に）なので、そこを止めると設計が壊れる。
      Gate の文言（「接続生成は1か所だけ」）どおり、**作る場所**を 1 つにした。

      窓口そのものは用途ごとに分ける必要がある ―― 1 本にすると戦記の返事と
      クエスト文の返事が同じ signal に混ざる。分けるべきは窓口で、
      揃えるべきは**作り方**だった（片方だけタイムアウトを直す、片方だけ
      `think` を切り忘れる、が実際に起きていた）。

      `LocalAI.create(parent)` と `LocalAI.enabled()` を足し、
      `main.gd` と `chronicle_ai.gd` はそれを呼ぶだけにした。
      `--no-ai` のときは null が返るので、**呼ぶ側は「窓口が無ければ頼まない」**
      だけでよく、AI の有無を各所で判定しなくて済む。

      **Gate:** `test_core.gd` に 6 件。`LocalAI.new()` がソース全体で 1 か所、
      それが `create()` の中、有無の判定も 1 か所。あわせて
      **AI の返事を適用する関数（`apply_to_world` / `apply_ai_skin`）が
      乱数もセーブも触らない**ことを見る ―― ここが触れば AI の有無で進行が
      ずれる（同じ種から同じ世界が出なくなる）。
      検査は**適用する関数だけ**に絞ってある。ファイル全体を見ると骨格側の
      `fallback` / `roll` が `DetRng` を使うので必ず引っかかるが、
      あちらは AI に関係なく回る決定的な側で、混ぜて見てはいけない。
      `--no-ai` を付けた起動でも撮影が通ることを確認。

## 2026-07-31（D-6 完了数をタスク ID から数える）

- [x] **[棚卸再開 D-6] 完了数をタスクIDから数える。**
      控えの `- ` 行を全部「完了件数」として数えていた。1 タスクが複数行の理由を
      持つので、**240 行を 240 件と言っていた**（実際は 12 件）。
      いま数えるのは `A-4` `C-10` のような ID の台帳。

      台帳が壊れていたら**終了コード 1** で落ちる。落ちる条件は 3 つ ――
      同じ ID が未完了と控えの両方に居る（移し忘れか二重着手）、
      同じ ID が控えに 2 回出てくる（二重記帳）、控えの項目に証跡が無い。

      **「〜の一部」と書いた項目はその ID を閉じない。** 大きなタスクの一部だけを
      先に済ませることがあり（C-8 の出入口だけを直した）、それで親を閉じると
      残りが黙って消える。

      **証跡の判定は「Gate:」というラベル探しにしなかった。** 走らせた道具、
      数字の結果、目で見たという記述 ―― どれも証跡である。ラベルだけを探すと、
      ちゃんと確かめてある古い項目まで落ちて、ラベルを貼るだけの作業になる。

      *導入して見つかったもの*: C-5 が控えに 2 回記帳されていた（衛生の関門が
      拾った移し忘れを、独立した項目として二重に書いていた）。証跡の無い項目が
      6 件（自分の分 5 件に後から書き足し、Codex の C-2 は後追いで確かめた）。

      **Gate:** `python tools/tasks_status.py --selftest` を足した。
      重複・両方在籍・証跡なしの各 fixture で落ち、正しい台帳では通り、
      「〜の一部」が親を閉じないことを見る（5 件）。本番も終了コード 0。

## 2026-07-31（C-8 TownProfile から町を組み立てる）

- [x] **[Codex追記 C-8] 町の設定を先に決め、間取り・施設・NPCへ一貫して反映する。**
      `TownProfile` が生業8種、支配4種、問題8種、各生業の目印と住人の役・台詞を
      間取りより先に `DetRng` で確定する。生成器はその設定から中央広場、南入口からの
      主街路、左右の宿・店、目印、装飾、住人を作る。同一世界の4町では Profile の組を
      重複させず、町名も入場通知だけでなく探索HUDへ残す。

      入口は南辺中央、開始位置は直上、内側5x3は主街路以外を置かない到着余白とした。
      主街路の最初の3歩には分岐も人物も置かず、広場・宿・店へ連続させる。
      固定入口や乱数装飾を除いた広場基準の内部指紋で多様性を測るため、寸法だけ違う町を
      別物として数えない。

      **Gate:** `test_core.gd` は成功476／失敗0。100町すべてで入口、到着余白、
      主街路、施設への到達性と案内看板、Profileと住人の接続を検査し、内部指紋50通り以上、
      16世界すべてで各4町のProfile非重複、同一入力の地形・町名・Profile一致を確認した。
      壊した主街路がGateに落ちる対照試験もある。`test_window_system.gd` は19／0、
      `check_ui.py` は33画面で違反0件。町の静止画を512x320で目視し、固有名・広場・
      宿・店・目印・住人が同時に判別できることを確認した。

      入力だけを送る60秒プレイでは町2回（滞在26秒）、宿2回、店2回、異なる住人4人、
      店4分類すべて、町からの退出2回を通過し、詰まりは0件だった。

## 2026-07-31（D-2 Sim を実プレイと同じランへ戻す）

- [x] **[棚卸再開 D-2] `balance.gd` を実プレイと同じランへ戻す。**
      Sim が通っていなかったのは**宿・宝箱・イベント・取得装備**だった。
      つまり「町の前を素通りし、初期装備のまま 10 階ぶんを戦う」測り方で、
      **実際より辛い**。そこで出た「妥当」を根拠に難度を語っていた。

      入れたもの:

      * **宿。** 町の上を通ったら全回復・毒治療（`main.gd` の `_on_inn` と同じ、
        無料）。実プレイで町の前を素通りする理由が無い。
      * **宝箱。** 階段までの道から 4 歩以内の箱だけ開ける。全部開けるのは
        寛容すぎる（部屋の隅までは寄らない）が、0 個だと初期装備のまま戦う
        ことになり、「深いほど勝てない」が実際より強く出る。
      * **拾った装備を着せる。** `BestGear.plan()` を通す ―― ここに別の判断を
        書くと、測っている強さと遊べる強さが別物になる。

      **イベントは入れていない。** `EventEffects` が `GameState` を要る作りで、
      Sim の素の辞書では渡せない。crude に真似ると「実システムを回す」という
      この Sim の前提が崩れるので、入れなかった。D-2 の残りとして正直に書く。

      *結果*: 全周が 到達 35% / 勝利 33% → **到達 71% / 勝利 63%**。
      **判定が「妥当」から「易しい」へ反転した。** 直す前が辛すぎたのであって、
      いまの数字のほうが遊んでいる状態に近い。

      **帯を外れたら終了コード 1** にした（到達 28〜38% / 勝利 10〜18%）。
      それまでは所見を print するだけで赤くならず、数字が動いても誰も止めない
      ので、「妥当」と言い続けているあいだに実際の難度がずれていく。

      途中で 1 つ踏んだ: **`quit()` は関数を抜けない。** `quit(1)` のあとに
      `quit()` が続いていて、赤いはずの回が緑で通っていた。`return` を足した。

      **Gate:** 帯の外で終了コード 1、帯の中で 0 になることを確認。
      いまは**赤い**（到達 71% / 勝利 63%）。難度を戻す作業は設計の判断なので
      D-8 として分けた。

## 2026-07-31（D-8 主を関門にする）

- [x] **[棚卸再開 D-8] 難度を帯の中へ戻す。**
      判定は `Encounter.EXP_GAIN を下げる` を指していたが、**採らなかった。**
      それは旅をさらに辛くする方向で、旅は既に唯一の関門だった。しかも
      経験値を絞ると戦闘数が増え、直したばかりの 1 戦の長さが相殺される。

      *測ったこと*。主戦だけを取り出す `--boss=N` を足した（世界を 1 周させると
      道中の乱れに埋もれて、主の強さを動かした効き目が読めない）。
      結果、**Lv14 の万全な party が 7 体の主すべてに 100% 勝っていた。**

      原因は数値ではなく**行動回数**だった。1 対 4 なので 23 手番の戦いで主が
      動けるのは 4 回ほど。しかもデータ側の `cost_scale` は 115〜160 と標準より
      **遅い** ―― 山場のはずの相手が、場でいちばん動けない駒だった。
      加えて主は階層補正を受けない作りで（`_to_battler` へ 1 を渡している）、
      party だけが装備と Lv で伸びていた。

      *直したこと*。`Encounter` に 3 つの定数を置いた ―― 能力の倍率 122%、
      速さの下限 27、行動の速さ 59%。**階層補正は掛けない**（調整点がデータと
      補正式の 2 か所に散る）。7 体の持ち味（重い機械は硬い、託宣は速い）は
      割合として残る。**感度が急峻**で、100/20/80 → 100%、140/30/40 → 2% と
      振り切れるので小刻みに詰めた。

      *結果*（500 ラン）:

          直行  到達 29%  勝利  0%   （封が効いている）
          封    到達 58%  勝利  7%
          全周  到達 72%  勝利 23%   ← 帯 45〜85 / 12〜25 の中

      **約半数のランが「着いたのに勝てない」**（72% 着いて 23% 勝つ）。
      狙っていた形になった。

      帯そのものも引き直した。前の帯（到達 28〜38 / 勝利 10〜18）は**壊れた Sim が
      出していた 35% から引かれていた** ―― 宿にも寄らず宝箱も開けない party を
      描写した数字で、そのまま守ると事故でできた辛さを設計として固定することになる。

      旧設計を記録していたテスト（「主に階層補正はかからない」）は、消さずに
      **新しい規則へ書き換えた**（主専用の倍率で伸びる／速さに下限がある／
      標準より速く動く）。

      **Gate:** `--runs=500` が終了コード 0。`test_core.gd` 478/0。
      `--shot=boss` 目視、`--play=120` 詰まりなし。
      手順と実測値は `.claude/skills/balance-tuning/SKILL.md` に残した。

## 2026-07-31（A-4 またぐ物語の配置を接続）

- [x] **A-4. 配置を接続する。**
      置き場は 6 つあるのに、**拠点と戦記の 2 つしか繋がっていなかった**。
      残り 4 つ（浅い町・中ほどの町・中ほどの洞・主戦の直前、計 27 段階）は
      カタログに在るのに一生出ない状態だった。しかも画面には何も出ないので
      気づけない ―― 物語が途中で止まっていても静かに進む。

      **出す口を 1 つにまとめた**（`_show_cross_world_beat`）。場所ごとに
      「探して出して進める」を書くと、片方だけ `advance` を忘れて段階が止まる。

      出し方は場所で変えた:

      * **町と洞は流す**（`toast`）。歩いている最中に窓で止めると、
        どこへ向かっていたか分からなくなる。
      * **主戦の直前は止める**（窓）。読ませる場面で、しかも読み終わってから
        戦いが始まる必要がある。`_close_outcome` が控えを見て扉を開け直す。

      `verify()` に**置き場の実在**も足した（`BEAT_BANDS`）。繋いでも、
      該当する帯の町や洞が無い世界ではやはり出ない。200 世界で引っかかり 0。

      **Gate:** `test_core.gd` に 3 件 ―― カタログの置き場がすべて `main.gd` で
      拾われていること（増やして繋ぎ忘れたら落ちる）、`verify` が置き場を
      見ていること。481/0。`--shot=boss` 目視、`--play=150` 詰まりなし。

## 2026-07-31（A-5 十二型 / A-6 結末を戦記へ）

- [x] **A-5. 十二型を解放する。**
      解放そのものは要らなかった ―― 白名簿の類は無く、12 型は最初から
      カタログに在った。**足りなかったのは「実際に出るか」の検査**。
      型を足しても、選出の条件（`min_runs_attempted` / `min_completed_arcs`）が
      厳しすぎると一生出ない型ができる。**カタログに在ることと、遊んでいて
      出会うことは別。**
      80 種 × 30 ランで回して、12 型すべてが選ばれ、同じ型が 1 度も続かない
      ことを確認（選出 300 回超）。`world_that_did_not_close` だけ出現が少ないが、
      これは意図どおり（`min_runs_attempted: 10` の終盤型）。
      **Gate:** `test_core.gd` に 4 件。型を足したのに出ない条件を書いたら落ちる。

- [x] **A-6. 結末を戦記へ、過去反響を最後に。**
      閉じた型の結末（`chronicle_line`）を戦記へ載せる口を作った。
      `line`（画面で読ませる文）と `chronicle_line`（記録に残す短い文）は
      用途が違うので、`advance()` は両方返す。

      **`world_that_did_not_close` はいちばん最後に置く。** あれは
      「閉じなかった世界」の話で、他の型の結末を受けたうえで読むもの。
      先に出ると、まだ起きていないことの後日談を先に読むことになる。

      並びは**決定的**（id 順に並べてから終幕を末尾へ）。乱数を引かない。
      毎ラン全部載せる ―― またぐ物語は何ラン越しの話なので、閉じたランでしか
      読めないと積み重ねが見えない。

      **Gate:** `test_core.gd` に 4 件。反響を先に閉じた状態を作って、
      それでも末尾に来ること、同じ状態からは同じ並びになること、
      戦記が受け取る口を持っていることを見る。488/0。`--shot=chronicle` 目視。

## 2026-07-31（B-4 画風を規約へ取り込む）

- [x] **B-4. `docs/character_art_direction.md` を規約へ取り込む。**
      要点を `AGENTS.md` と `HERO_LOOKS` の両方へ写した。**外しやすいのは
      「職業名から定番記号を足す」こと**なので、そこを先頭に置いた ――
      遊び人に二股帽、銃士に制帽、錬金術師に白衣、魔獣使いに獣耳。
      名前から連想したものを足した時点で、この作品の人ではなくなる。

      代わりに役割を作品語彙へ翻訳する（黒い漆・象牙・骨、有機的な角や襟、
      一系統だけの強い差し色、24x32 では輪郭を 2〜3 個残す）。

      `HERO_LOOKS` の頭にも同じ規約を置いた。**生成器を触る人が最初に読む場所**が
      そこなので、`AGENTS.md` にだけ書いても届かない。

      あわせて描き分けの測り方も規約へ書いた ―― 生の重なりでは 15 職すべて人型で
      0.64〜0.85 に固まる。共通の胴体を差し引いた出っ張りだけを比べる。
      以前ここが平均色を測っていて、間違った組を名指ししていた。

      **Gate:** `python tools/gen_assets.py` が通り、描き分けの報告が出る
      （現在 8 組）。`check_assets.py` 0 件、`test_core.gd` 488/0。
