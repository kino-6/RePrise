# Godot
.godot/
/android/
export_presets.cfg
*.translation

# 生成物: 音声だけはサイズが大きいので追跡しない。
# 生成器（tools/gen_audio.py）が唯一の原本で、セットアップ時に作り直す。
# ドット絵は数 KB なので逆に追跡する（差分がレビューできる利点が勝る）。
assets/audio/**/*.wav

# 開発用の撮影物
docs/preview/screen_*.png

# OS
Thumbs.db
desktop.ini
